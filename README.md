<div align="center">

# 🛡️ IRNetFree

**یک کلاینت دسکتاپ تمیز، مدرن و امن برای اجرای کانفیگ‌های Xray**
پروکسی • تانل کل‌سیستم (TUN) • روتینگ پیشرفته • زنجیره پروکسی • تست پینگ • بررسی IP

A clean, modern, cross‑platform desktop **VPN client** built on **Electron**, driving **xray‑core**.
Windows · macOS (Intel + Apple Silicon) · Linux

</div>

---

## 📑 فهرست (Table of Contents)

- [امکانات کامل](#-امکانات-کامل-features)
- [نصب و اجرا](#-نصب-و-اجرا-setup)
- [شروع سریع](#-شروع-سریع-quick-start)
- [افزودن کانفیگ‌ها](#-افزودن-کانفیگها-adding-configs)
- [ساب‌اسکریپشن](#-ساباسکریپشن-subscriptions)
- [دو هسته‌ی Xray](#-دو-هستهی-xray-cores)
- [پین‌کردن گواهی](#-پینکردن-گواهی-certificate-pinning)
- [حالت اتصال: پروکسی یا TUN](#-حالت-اتصال-پروکسی-یا-tun-connection-mode)
  - [لایه‌ی تونل: sing-box (یا tun2socks)](#لایهی-تونل-sing-box-یا-tun2socks)
  - [گارد نشتی (Leak Guard)](#گارد-نشتی-leak-guard)
  - [بستن UDP در حالت پروکسی](#بستن-udp-در-حالت-پروکسی)
- [روتینگ](#-روتینگ-routing)
  - [روتینگ ساده](#۱-روتینگ-ساده-simple)
  - [قوانین سفارشی](#۲-قوانین-سفارشی-custom-rules)
  - [روتینگ پیشرفته گرافیکی](#۳-روتینگ-پیشرفته-گرافیکی-advanced)
  - [روتینگ بر اساس پروسه](#۴-روتینگ-بر-اساس-پروسه-process)
- [زنجیره پروکسی](#-زنجیره-پروکسی-proxy-chains)
- [وایرگارد شرکتی](#-وایرگارد-شرکتی-corporate-wireguard)
- [استخر پروکسی (چند کانفیگ هم‌زمان)](#-استخر-پروکسی-multi-proxy-pool)
- [DNS](#-dns)
- [اشتراک در شبکه محلی (LAN)](#-اشتراک-در-شبکه-محلی-lan)
- [کیل‌سوییچ](#-کیلسوییچ-kill-switch)
- [اجرای با دسترسی ادمین](#-اجرای-با-دسترسی-ادمین-run-as-admin)
- [اولین اجرا روی مک](#-اولین-اجرا-روی-مک-macos--first-run)
- [فایل‌های موردنیاز](#-فایلهای-موردنیاز-required-files)
- [نسخه و به‌روزرسانی](#-نسخه-و-بهروزرسانی-version--update)
- [آمار و تست](#-آمار-و-تست-stats--testing)
- [ظاهر و رابط کاربری](#-ظاهر-و-رابط-کاربری-ui)
- [مرجع تنظیمات](#-مرجع-تنظیمات-settings-reference)
- [ساخت نسخه نصبی](#-ساخت-نسخه-نصبی-build)
- [نسخه CLI / سرور بدون GUI (لینوکس)](#-نسخه-cli--سرور-بدون-gui-لینوکس-headless)
- [نسخه اندروید](#-نسخه-اندروید-android)
- [انتشار خودکار (GitHub Actions)](#-انتشار-خودکار-github-actions-release)
- [معماری](#-معماری-architecture)
- [ساختار پروژه](#-ساختار-پروژه-project-structure)
- [امنیت](#-امنیت-security)
- [عیب‌یابی](#-عیبیابی-troubleshooting)

---

## ✨ امکانات کامل (Features)

| دسته | توضیح |
|---|---|
| **پروتکل‌ها** | VLESS (با Reality / XTLS‑Vision)، VMess، Trojan، Shadowsocks، **WireGuard**، و **SOCKS5 / HTTP پروکسی** |
| **دو هسته‌ی Xray** | هسته‌ی رسمی (`XTLS/Xray-core`) و fork پترنیها (`patterniha/Xray-core`، کانفیگ‌های بدون TLS به آدرس عمومی را رد نمی‌کند). انتخاب per-config در فرم ویرایش، «هسته‌ی پیش‌فرض» در تنظیمات، هر دو از «فایل‌های موردنیاز» دانلود/به‌روزرسانی می‌شوند. یک زنجیره/استخر/نقشه‌ی پیشرفته اگر **حتی یکی** از سرورهایش فورک بخواهد، کلش روی فورک اجرا می‌شود → [دو هسته‌ی Xray](#-دو-هستهی-xray-cores) |
| **افزودن کانفیگ** | لینک تکی، چند لینک هم‌زمان، متن base64، لینک ساب‌اسکریپشن، **فایل یا متنِ `.conf` وایرگارد**، فرم دستی WireGuard، **فرم دستی SOCKS/HTTP**، لینک `socks://`، و **چسباندن سریع با Ctrl+V** در هر جای برنامه |
| **اشتراک‌گذاری کانفیگ** | ساخت دوباره‌ی لینک از روی هر کانفیگ ذخیره‌شده، برای کپی یا **کد QR** |
| **پین‌کردن گواهی** | `allowInsecure` از هسته حذف شده؛ به‌جایش گواهیِ سرور در اولین استفاده پین می‌شود (`pinnedPeerCertSha256`) — سخت‌گیرانه‌تر از چیزی که `allowInsecure` بود، و تغییر گواهی خودکار تشخیص داده می‌شود |
| **ساب‌اسکریپشن** | افزودن، به‌روزرسانی دستی/خودکار، و نمایش **حجم مصرف و زمان باقی‌مانده با پروگرس‌بار** |
| **گروه‌بندی سرورها** | لیست به تفکیک منبع: «کانفیگ‌های دستی» اول، بعد هر ساب با نام و تعدادِ خودش. کانفیگ انتخاب‌شده در لیست و در انتخابگر صفحه‌ی خانه هم‌زمان پررنگ می‌شود و انتخاب از هر طرف در دیگری هم اعمال می‌شود |
| **مصرف کل هر کانفیگ** | حجمی که تا امروز از هر سرور/زنجیره/ورودی استخر رفته، ماندگار در برابر قطع‌ووصل و بستن برنامه (فایل جدای `usage.json`). با کلیک روی همان عدد پاک می‌شود، یا یکجا از دکمه‌ی «پاک‌کردن مصرف» |
| **کارت‌های تنظیمات** | گزینه‌هایی که پیامد دارند (بک‌اندِ TUN، سطح گارد نشتی، حالت اتصال) به‌جای دراپ‌داونِ خالی، کارت با آیکون و توضیح‌اند؛ اگر بک‌اند نصب نباشد همان‌جا می‌گوید چه چیزی را نصب کنید |
| **پروکسی محلی** | SOCKS5 + HTTP inbound با پورت‌های قابل تنظیم |
| **استخر پروکسی (Multi‑Proxy)** | اجرای **چند کانفیگ/زنجیره/ساکس هم‌زمان**، هرکدام روی پورت محلی جدا (مثلاً 60001، 60002 …) — سازگار با هر دو حالت پروکسی و TUN |
| **پروکسی سیستمی** | تنظیم خودکار پروکسی سیستم‌عامل هنگام اتصال |
| **حالت TUN** | **پیش‌فرض.** تونل کردن **کل ترافیک سیستم** با sing-box (یا tun2socks به‌عنوان جایگزین قدیمی) روی Windows/macOS/Linux |
| **گارد نشتی (Leak Guard)** | در حالت TUN: DNS همه‌ی کارت‌های شبکه از تونل می‌رود (سطح استاندارد) و در سطح سخت‌گیرانه هر چیزی که از تونل نمی‌رود بسته می‌شود؛ در حالت پروکسی، گزینه‌ی بستن UDP جلوی لو رفتن IP با WebRTC را می‌گیرد |
| **روتینگ** | ساده (گلوبال/دور زدن ایران/چین/مستقیم) + قوانین سفارشی + **روتینگ پیشرفته گرافیکی** (که می‌تواند حالت ساده را هم زیرِ قانون‌هایش اعمال کند) |
| **روتینگ پراسس** | هدایت ترافیک یک **برنامه‌ی خاص** به یک سرور/زنجیره دلخواه |
| **زنجیره پروکسی** | عبور ترافیک از چند سرور پشت‌سرهم (مثلاً برای دسترسی به دیتابیس از طریق WireGuard) |
| **وایرگارد شرکتی** | فایل `.conf` را می‌خواند، **DNS و دامنه‌های جست‌وجوی** داخلش را نگه می‌دارد و از **داخلِ همان تونل** می‌پرسد، و `AllowedIPs` را به‌عنوان **پیشنهاد قانون روتینگ** جلوی شما می‌گذارد؛ تونلی که handshake اش کامل نشود، خودش با صدای بلند می‌گوید |
| **DNS** | DNS خارجی روی DoH از داخل تونل + DNS داخلی (شکن/الکترو…) برای «دور زدن ایران» + resolverهایی که به یک مقصد تعلق دارند و از همان مقصد پرسیده می‌شوند؛ هر DNS ای که به هسته برسد همان‌جا جواب داده می‌شود |
| **اشتراک LAN** | تبدیل این دستگاه به پروکسی برای بقیه دستگاه‌های شبکه |
| **کیل‌سوییچ** | قطع کامل اینترنت در صورت افت ناگهانی اتصال (جلوگیری از نشتی) |
| **بازیابی هنگام تغییر شبکه** | جابه‌جایی Wi-Fi↔اترنت، lease تازه، بیدارشدن از sleep یا لینکی که قطع شود و برگردد، هسته را نمی‌کشد ولی سوکت‌هایش را می‌شکند؛ برنامه این را می‌بیند و اتصال را **بدون رها کردنِ گارد نشتی** بازمی‌سازد — دکمه‌ی دستی هم روی صفحه‌ی خانه هست |
| **آمار به تفکیک کانفیگ** | نمودار مسیر روی صفحه‌ی خانه می‌گوید هر قانون/هاپ چقدر ترافیک برده |
| **به‌روزرسانی** | بررسی نسخه‌ی جدید از گیت‌هاب و **دانلود نصب‌کننده از داخل برنامه** با تأیید checksum؛ نمایش نسخه‌ی هسته‌ها؛ **به‌روزرسانی هفتگی خودکار فایل‌های geo** (و هسته‌ها، اگر بخواهید) فقط وقتی وصل نیستید |
| **تست** | پینگ TCP، **تأخیر دانلود و آپلود** از داخل تونل، بررسی IP/کشور خروجی، و اعتبارسنجی کدهای `geoip:`/`geosite:` از خودِ هسته هنگام ذخیره‌ی قانون‌ها |
| **آمار زنده** | سرعت و حجم دانلود/آپلود لحظه‌ای + **نمودار شصت‌ثانیه‌ای سرعت** زیر شمارنده‌ها |
| **ظاهر** | تم دارک، رابط دو‌زبانه (فارسی RTL / English)، رنگ‌بندی وضعیت اتصال، نشان روی آیکون تسک‌بار، **سینی سیستم با فهرست سرورها به تفکیک ساب** (یک کلیک = اتصال)، **اعلان سیستمی** برای قطع/اتصال مجدد/کیل‌سوییچ |
| **اجرا با سیستم‌عامل و اتصال خودکار** | پنهان در سینی، هنگام ورود به سیستم (روی ویندوز با task زمان‌بندی‌شده — چون برنامه ادمین است و login item معمولی اجرا نمی‌شود) و اتصال به آخرین سرور در اجرا؛ هر دو پیش‌فرض خاموش |
| **انتخاب خودکار سریع‌ترین** | ردیف «خودکار» در انتخابگر: همه را تست می‌کند و به کم‌تأخیرترین وصل می‌شود |
| **پشتیبان‌گیری / بازیابی** | همه‌ی سرورها، ساب‌ها، زنجیره‌ها، استخر، تنظیمات و مصرف در یک JSON؛ بازیابی ادغام است، نه جایگزینی |
| **نسخه‌ی سرور (Headless/CLI)** | اجرای همین برنامه روی **لینوکسِ بدون GUI**؛ همان UI روی یک پورت وب بالا می‌آید (نصب تک‌خطی، دسترسی با SSH-forward) — [راهنما](#-نسخه-cli--سرور-بدون-gui-لینوکس-headless) |

---

## 🚀 نصب و اجرا (Setup)

**پیش‌نیاز:** Node.js 18+ (برای اجرای `npm test` به Node **21+** نیاز است — `node --test` تا پیش از آن الگوی `tests/*.test.js` را نمی‌فهمید؛ CI روی Node 22 اجرا می‌شود)

```bash
git clone https://github.com/sadrazkh/Irnetfree_xray-client.git
cd Irnetfree_xray-client      # یا پوشه‌ی xray-client
npm install

# دانلود خودکار آخرین هسته xray-core در پوشه‌ی bin/
npm run get-xray
# یا: از داخل برنامه → تنظیمات → فایل‌های موردنیاز → دانلود

npm start
```

> نیازی به نصب دستی هسته نیست؛ اگر چیزی کم باشد، **هنگام باز شدن برنامه** پنجره‌ای ظاهر می‌شود و با اجازه‌ی شما فایل‌های لازم را دانلود می‌کند.

---

## ⚡ شروع سریع (Quick Start)

۱. برنامه را باز کنید؛ اگر هسته‌ی Xray نصب نیست، روی **«دانلود کن»** بزنید.
۲. به تب **سرورها** بروید و کانفیگ خود را اضافه کنید (یا هرجای برنامه **Ctrl+V** بزنید).
۳. به صفحه‌ی **اتصال** برگردید، از منوی بازشو سرور را انتخاب کنید — یا ردیف **«خودکار (سریع‌ترین)»** را بزنید تا همه تست شوند و به بهترین وصل شوید.
۴. دکمه‌ی بزرگ روشن/خاموش وسط را بزنید.
۵. وقتی **تولبار و لوگو سبز** شد یعنی متصل‌اید ✅ — برای اطمینان «بررسی IP» را بزنید.

---

## 🔗 افزودن کانفیگ‌ها (Adding Configs)

در تب **سرورها** روی «+ افزودن» بزنید و یکی از موارد زیر را بچسبانید (برنامه نوعش را خودکار تشخیص می‌دهد):

**۱) لینک تکی:**
```
vless://uuid@example.com:443?security=reality&pbk=...&sni=...#MyServer
```

**۲) چند لینک با هم (هر خط یک کانفیگ):**
```
vless://...#Server-1
vmess://...
trojan://...@host:443#Server-3
ss://...#Shadowsocks-1
```

**۳) متن base64 (خروجی خام ساب):** کل بلوک base64 را بچسبانید، خودکار دیکد و وارد می‌شود.

**۴) لینک ساب‌اسکریپشن:** هر خطی که با `http(s)://` شروع شود به‌عنوان ساب اضافه و دریافت می‌شود (به بخش بعد).

**۵) SOCKS/HTTP پروکسی:** یا لینک `socks://user:pass@host:port#name` (و `socks5://`) را در «افزودن» بچسبانید، یا دکمه‌ی **«+ SOCKS/HTTP»** را بزنید و فرم را پر کنید (نوع، host، پورت، و در صورت نیاز نام‌کاربری/رمز). این پروکسی مثل هر کانفیگ دیگری قابل انتخاب، پینگ، زنجیره و استفاده در استخر است.

**۶) WireGuard:** دکمه‌ی «+ WireGuard» → یا **«انتخاب فایل .conf»** را بزنید تا فرم خودش پر شود، یا کل متنِ کانفیگ (بلوکِ `[Interface]` + `[Peer]`) را در همان کادرِ «افزودن» بچسبانید، یا دستی پرش کنید.
> 💡 فیلد **Endpoint** باید آدرس عمومی سرور به شکل `host:port` باشد (مثل `de.example.com:51820`)، نه آدرس محلی تونل. فیلد **Address** آدرس محلی شما و حتماً `/32` است (مثل `10.8.0.2/32`). برای دسترسی به یک رنج خاص، آن را در **Allowed IPs** بگذارید (مثل `192.168.60.0/24`) یا `0.0.0.0/0` را نگه دارید.
> فیلد **DNS** هم هست: اگر کانفیگ شرکتی خط `DNS =` دارد همان را بگذارید (`192.168.60.1`) و دامنه‌های جست‌وجو را با کاما بعدش (`tes.systems`). → [وایرگارد شرکتی](#-وایرگارد-شرکتی-corporate-wireguard)

> ⚡ **میان‌بر:** هرجای برنامه (بیرون از کادرهای متنی) **Ctrl+V** بزنید تا کانفیگ یا لینک ساب از کلیپ‌بورد فوراً اضافه شود.

ویرایش: روی آیکون ✎ هر سرور بزنید تا پروتکل، ترنسپورت، TLS/Reality، کلیدها و … را تغییر دهید.

---

## 📡 ساب‌اسکریپشن (Subscriptions)

در تب **ساب‌ها**:

- **افزودن ساب:** آدرس `https://...` و یک نام دلخواه بدهید.
- **به‌روزرسانی:** دستی (آیکون ⟳) یا **خودکار** با فاصله‌ی دلخواه (مثلاً هر ۶۰ دقیقه).
- **حجم و زمان:** اگر سرور ساب هدر استاندارد `Subscription-Userinfo` بفرستد، روی کارت ساب دو نوار نمایش داده می‌شود:
  - 🔵 **حجم مصرف:** «۱۲٫۳ GB / ۵۰ GB · ۲۵٪» با نوار رنگی (سبز → زرد → قرمز با نزدیک شدن به سقف).
  - 🟢 **زمان باقی‌مانده:** «۱۸ روز مانده» یا «منقضی شده».

> با حذف یک ساب، سرورهای متعلق به آن هم پاک می‌شوند؛ سرورهایی که دستی اضافه کرده‌اید دست‌نخورده می‌مانند.

---

## 🧬 دو هسته‌ی Xray (Cores)

برنامه یک «رجیستری هسته» دارد ([`src/main/engines.js`](src/main/engines.js)) و سه هسته در آن ثبت شده‌اند. هر سه در پوشه‌ی داده‌ها کنار هم زندگی می‌کنند، چون فقط نامِ فایل اجراشان فرق دارد:

| هسته | فرمت کانفیگ | از کجا دانلود می‌شود | نام فایل |
|---|---|---|---|
| **Xray (رسمی)** | Xray JSON | `XTLS/Xray-core` | `xray.exe` / `xray` |
| **Xray-PattN (پترنیها)** | همان Xray JSON | `patterniha/Xray-core` | `xray-pattn.exe` / `xray-pattn` |
| **sing-box** | فرمت خودِ sing-box | (همان باینریِ حالت TUN) | `sing-box.exe` / `sing-box` |

فورک پترنیها دقیقاً همان بالادست است به‌علاوه‌ی یک تفاوت: چکِ `validateOutboundTransportSecurity` را ندارد، یعنی کانفیگ VLESS/Trojan **بدون TLS** به یک آدرس عمومی (مثل ws روی پورت ۸۰ پشت CDN) را رد نمی‌کند. چون فرمت کانفیگ و آرگومان‌های خط فرمانش یکی است، همان `config.json` روی هر دو اجرا می‌شود.

### کدام هسته یک اتصال را اجرا می‌کند

انتخاب در [`src/main/engineChoice.js`](src/main/engineChoice.js) است و یک قاعده‌ی ساده دارد:

| نوع اتصال | هسته |
|---|---|
| **تک‌کانفیگ** | هسته‌ای که در فرم ویرایشِ همان کانفیگ انتخاب کرده‌اید؛ اگر انتخاب نکرده باشید، **هسته‌ی پیش‌فرض** از تنظیمات |
| **زنجیره / استخر / روتینگ پیشرفته** | اگر **حتی یکی** از سرورهایی که در آن نقشه شرکت دارند PattN بخواهد، **کل نقشه** روی PattN اجرا می‌شود؛ وگرنه هسته‌ی پیش‌فرض |

منطقش این است: PattN هرچه هسته‌ی رسمی قبول می‌کند را قبول می‌کند به‌علاوه‌ی کانفیگ‌های بدون TLS. پس زنجیره‌ای که فقط **یک** هاپ بدون TLS دارد باید کل‌اش روی PattN برود.

- **sing-box فقط برای تک‌کانفیگ است** — مترجمی برای زنجیره و استخر ندارد. اگر روی یک کانفیگِ داخل زنجیره انتخابش کنید نادیده گرفته می‌شود، و اگر باینری‌اش نباشد به Xray برمی‌گردد و در لاگ می‌نویسد چرا.
- **تست تأخیر** همیشه روی یکی از دو هسته‌ی فرمت‌ِ Xray اجرا می‌شود (کانفیگ تست، Xray JSON است).
- انتخاب هسته **per-config** است: عوض‌کردن هسته‌ی یک کانفیگ به هیچ کانفیگ دیگری و به کل برنامه کاری ندارد.

### برگشت خودکار به PattN

اگر هسته‌ی رسمی کانفیگی را با پیام `without TLS ... prohibited` رد کند و PattN نصب باشد، برنامه همان کانفیگ را روی PattN اعتبارسنجی و اجرا می‌کند و در لاگ می‌نویسد:

```
Official core rejects this plaintext config — running it on Xray-PattN (patterniha)
```

اگر PattN نصب **نباشد**، پیام خطا خودش می‌گوید چه کار کنید: «این کانفیگ TLS ندارد و هستهٔ رسمی آن را رد می‌کند. Xray-PattN را از تنظیمات → فایل‌های موردنیاز نصب کن.»

> ℹ️ **این مسیر فعلاً خوابیده است.** *ریلیزِ* هسته‌ی رسمی (تا `26.3.27` که این متن با آن سنجیده شده) هنوز آن چک را ندارد و کانفیگ‌های بدون TLS را خودش اجرا می‌کند؛ چک فقط روی شاخه‌ی `main` بالادست است. برگشت خودکار از روزی به کار می‌آید که بالادست ریلیزی از `main` منتشر کند — تا آن روز فقط آماده نشسته است.

### تفاوت‌های دیگری که به چشم می‌آیند

- **Endpoint وایرگارد:** هسته‌ی رسمی نامِ endpoint را با DNS خودش (DoH، از داخل تونل) resolve می‌کند. فورک این کار را نمی‌کند — مستقیم که باشد از resolver سیستم‌عامل می‌پرسد (`Unable to update bind: lookup <host>: no such host`) و از داخل زنجیره، نام خام را دست هاپ بعدی می‌دهد. برنامه فقط **برای فورک** نام را از قبل به آدرس تبدیل می‌کند؛ به هسته‌ی رسمی دست نمی‌زند تا lookup مقاومِ سانسورش را با جواب resolver محلی عوض نکند. → [وایرگارد شرکتی](#-وایرگارد-شرکتی-corporate-wireguard)
- **قابلیت‌های مخصوصِ فورک** — `finalMask` (ماسک‌کردن ترنسپورت: fragment / noise / هدر سفارشی) و `cipherSuites` — در فرم ویرایش قابل تنظیم‌اند و در لینک اشتراکیِ خروجی هم می‌آیند (`fm=`). هسته‌ی رسمی `finalMask` را نمی‌شناسد و کانفیگ را با `LengthMin can't be 0` رد می‌کند، پس چنین کانفیگی باید روی PattN اجرا شود.

---

## 🔏 پین‌کردن گواهی (Certificate Pinning)

**`allowInsecure` از هر دو هسته حذف شده است.** اگر کانفیگی آن را داشته باشد، هسته کانفیگ را **هنگام بارگذاری** رد می‌کند (`removed and migrated to pinnedPeerCertSha256`) — یعنی اتصال اصلاً شروع نمی‌شود. و از آن‌جا که لینک‌های اشتراکیِ ایرانی مدام `allowInsecure=1` دارند، این یعنی خیلی از کانفیگ‌های سالم از کار می‌افتادند.

جایگزینش **پین‌کردن گواهی در اولین استفاده** است ([`src/main/certPin.js`](src/main/certPin.js)):

۱. اولین باری که به چنین سروری وصل می‌شوید، برنامه یک TLS handshake می‌زند و **SHA-256 گواهیِ برگ** را می‌خواند.
۲. آن هش روی رکورد سرور ذخیره می‌شود (`certPin` و زمانش `certPinAt`).
۳. از آن به بعد، به‌جای `allowInsecure`، همان هش به‌شکل `pinnedPeerCertSha256` داخل کانفیگ نوشته می‌شود.

نتیجه: هسته **همان یک گواهی و هیچ گواهی دیگری** را قبول می‌کند — که سخت‌گیرانه‌تر از کاری است که `allowInsecure` هرگز می‌کرد. `allowInsecure` هیچ‌وقت — نه `true` نه `false` — داخل کانفیگ نوشته نمی‌شود.

| موقعیت | چه اتفاقی می‌افتد |
|---|---|
| **سروری که خودِ دستگاه با آن دست می‌دهد** | خودکار پین می‌شود: تک‌کانفیگ، **هاپ اولِ** زنجیره، و در استخر/روتینگ پیشرفته سرورِ هر مقصد (یا هاپ اولِ زنجیره‌ی آن مقصد). |
| **سروری که پشت یک هاپ دیگر است** | از این‌جا قابل‌دیدن نیست. در لاگ می‌نویسد «برای پین‌شدن، یک‌بار مستقیم به آن وصل شو» — همان یک‌بار کافی است و بعد در زنجیره هم کار می‌کند. |
| **گواهی سرور عوض شده** | پیش از هر اتصال، پین‌های ذخیره‌شده یک‌بار با خودِ سرور مقایسه می‌شوند. پینِ کهنه پاک می‌شود، پیام «Certificate changed for …» در لاگ می‌آید و گواهیِ تازه در همان اتصال پین می‌شود. سروری که فقط **در دسترس نیست** دست‌نخورده می‌ماند — «در دسترس نبودن» یعنی «عوض شده» نیست. |
| **handshake ما جواب نمی‌گیرد** | (مثلاً سروری که فقط uTLS را می‌پذیرد) پین خوانده نمی‌شود، در لاگ نوشته می‌شود، و اعتبارسنجی گواهی می‌افتد گردنِ خودِ هسته — خطای خودش سیگنالِ شماست. |

- در فرم ویرایش هر سرور، **«گواهی پین‌شده»** و دکمه‌ی **«پاک کردن پین»** هست؛ با پاک‌کردنِ پین، اتصال بعدی دوباره پین می‌کند.
- **لینک اشتراکیِ خروجی** همچنان `allowInsecure=1` را نگه می‌دارد تا با بقیه‌ی کلاینت‌ها سازگار بماند؛ این فقط داخل کانفیگی که به هسته داده می‌شود حذف شده.
- هسته وقتی گواهیِ ناجور ببیند، آن را **فقط در سطح لاگ `info`** می‌گوید (`peer cert is unrecognized (against pinnedPeerCertSha256)`) و برنامه روی `warning` اجرا می‌شود — به همین دلیل مقایسه‌ی پیش از اتصال وجود دارد، وگرنه سروری که گواهی‌اش چرخیده بود برای همیشه از کار می‌افتاد.

---

## 🔌 حالت اتصال: پروکسی یا TUN (Connection Mode)

روی کارت حالت در صفحه‌ی اتصال بزنید تا بین دو حالت سوییچ کنید:

| حالت | چه‌چیزی تونل می‌شود؟ | نیاز به ادمین | کاربرد |
|---|---|---|---|
| ⚡ **پروکسی** | فقط برنامه‌هایی که از پروکسی سیستمی/SOCKS استفاده می‌کنند (مرورگرها، …) | خیر | سبک و سریع، اکثر کارهای روزمره |
| 🛡️ **TUN** | **کل ترافیک سیستم** (همه‌ی برنامه‌ها) | بله | وقتی برنامه‌ای پروکسی را نمی‌خواند یا تونل کامل لازم است |

> در حالت TUN روی ویندوز به `sing-box` (یا `tun2socks`) و `wintun.dll` و **دسترسی ادمین** نیاز است. اگر ادمین نباشید، برنامه پیشنهاد می‌دهد خودش را با دسترسی ادمین دوباره اجرا کند.
> روی مک، هنگام اتصال **یک‌بار** پنجره‌ی **رمز عبور** نمایش داده می‌شود و تونل کامل برپا می‌شود.

### لایه‌ی تونل: sing-box (یا tun2socks)

کارتِ شبکه‌ی مجازی را یکی از این دو می‌سازد. **تنظیمات → بک‌اندِ TUN**:

| بک‌اند | چه می‌کند |
|---|---|
| **sing-box** (پیش‌فرض، پیشنهادی) | خودش کارت `IRNetFree` را بالا می‌آورد و روت‌های **IPv4 و IPv6** را می‌گذارد (`auto_route`)، پس v6 از تونل بیرون نمی‌زند. IP سرورها و DNSهای bypass را از تونل مستثنی می‌کند. هنگام خروج (حتی با kill) **خودش روت‌ها و فیلترهایش را پاک می‌کند**. DNS کارت تونل روی `172.19.0.2` (و در حالت IPv6 روی `fdfe:dcba:9876::2`) می‌نشیند و از داخل تونل به هسته می‌رسد. |
| **tun2socks** (قدیمی) | همان کار با روت‌های `0.0.0.0/1` + `128.0.0.0/1`؛ فقط IPv4 و پاک‌سازی‌اش دست برنامه است. برای سازگاری نگه داشته شده. |

- **انتخاب واقعی:** اگر sing-box نصب باشد از آن استفاده می‌شود؛ اگر نه، به tun2socks برمی‌گردد و **در لاگ‌ها می‌نویسد چرا**. اگر خودتان `tun2socks` را انتخاب کنید، همان اجرا می‌شود.
- تغییر بک‌اند مثل بقیه‌ی تنظیماتِ تونل، **با اتصال مجدد** اعمال می‌شود (برنامه خودش می‌پرسد).
- **چرا «مستقیم» زیر TUN واقعاً مستقیم است:** وقتی TUN بالا باشد، مسیر پیش‌فرضِ سیستم خودِ تونل است — پس هر اتصالی که *خودِ هسته* برقرار می‌کند (خروجی `direct`، هاپ اولِ زنجیره، endpoint وایرگارد، دیالرهای ضدِ DPI) دوباره وارد تونل می‌شد و در حلقه می‌افتاد. برنامه پیش از بالا آوردن تونل، نام کارت شبکه‌ی فیزیکی را می‌خواند و این سوکت‌ها را به همان کارت می‌بندد. اگر نتواند کارت را پیدا کند، هشدار می‌دهد.
- روی مک نام کارت را کرنل می‌دهد (`utun0`، `utun3`، …) و برنامه دستگاهِ تازه‌ظاهرشده را پیدا و در لاگ ثبت می‌کند.

### گارد نشتی (Leak Guard)

خودِ TUN، *مسیر* را می‌گیرد ولی به‌تنهایی جلوی همه‌ی نشتی‌ها را نمی‌گیرد (کارت‌های شبکه‌ی فیزیکی DNS خودشان را دارند، WebRTC مستقیم UDP می‌زند). **تنظیمات → گارد نشتی** سه سطح دارد و **فقط در حالت TUN** معنی دارد:

| سطح | چه چیزی را می‌بندد |
|---|---|
| **خاموش** | هیچ؛ فقط خود TUN. |
| **استاندارد** (پیش‌فرض) | DNS **همه‌ی کارت‌های شبکه‌ی فیزیکی** (v4 و v6) روی آدرس تونل گذاشته می‌شود، پس هیچ برنامه‌ای نمی‌تواند از resolverِ مودم/ISP بپرسد. مقدارهای اصلی قبل از تغییر ذخیره و هنگام قطع/خروج برگردانده می‌شوند. |
| **سخت‌گیرانه** | همه‌ی موارد بالا + هر چیزی که **از تونل نمی‌رود** بسته می‌شود: DNS معمولی روی پورت ۵۳ بیرون از تونل، کل IPv6 بیرون از تونل، و UDPِ خارج از تونل (یعنی **WebRTC/STUN**). امن‌ترین حالت، ولی می‌تواند بازی‌ها و تماس‌های تصویری را بشکند. |

**چرا سطح استاندارد لازم است** (خودِ TUN کافی نیست):

- **ویندوز** یک پرس‌وجوی DNS را هم‌زمان به resolverِ **همه‌ی** کارت‌های متصل می‌فرستد (*smart multi-homed name resolution*) و اولین جوابی که برسد برنده است — یعنی ISP همچنان هر نامی را که دستگاه می‌پرسد می‌بیند.
- **مک** به‌ازای هر «سرویس شبکه» resolve می‌کند و فقط سرویسی که مسیر پیش‌فرض را دارد مالِ ماست.

**دقیقاً چه کار می‌کند، روی هر سیستم‌عامل** ([`src/main/leakGuard.js`](src/main/leakGuard.js)):

| | ویندوز | مک |
|---|---|---|
| **استاندارد** | DNS هر کارت **فیزیکی** (v4 و v6) روی آدرس تونل نوشته می‌شود؛ برای هر عملیات یک بار `powershell` بالا می‌آید، نه یک بار برای هر کارت. مقدارهای اصلی — با این جزئیات که کدام‌شان DHCP بوده‌اند — پیش از تغییر ذخیره می‌شوند، و در پایان کش DNS هم پاک می‌شود. | با `networksetup -setdnsservers` روی **همه‌ی سرویس‌های شبکه**. چون این دستور root می‌خواهد، هنگام اتصال **یک پنجره‌ی رمز دوم** (جدا از خود تونل) باز می‌شود. |
| **سخت‌گیرانه** | علاوه بر بالا: `strict_route` روی sing-box + قانون‌های فایروال روی هر کارت فیزیکی (TCP و UDP) که هر آدرسی به‌جز استثناها را می‌بندند. همه‌ی قانون‌ها زیر گروهِ `IRNetFree` ساخته می‌شوند و فقط با همان گروه پاک می‌شوند. | یک anchor به نام `irnetfree` در `pf`. **آزمایشی** — هنوز روی مک واقعی آزمایش نشده و خودِ رابط هم این را می‌گوید. اگر نام دستگاه تونل پیدا نشود، بلوک اصلاً اعمال نمی‌شود و در لاگ می‌گوید. |

- **کارت‌های خودمان هیچ‌وقت گارد نمی‌شوند** (`IRNetFree` و کارت tun2socks)، و روی ویندوز کارت‌های مجازی (TAP / Hyper-V / VMware / VirtualBox / Bluetooth / Loopback) هم «فیزیکی» حساب نمی‌شوند.
- **روی لینوکس گاردی وجود ندارد** — resolver قابل‌اتکایی برای بازنویسی نیست؛ برنامه در لاگ می‌گوید DNS را خودتان روی آدرس تونل بگذارید.
- روی مک، `pf` را اگر خودش روشن کرده باشد فقط خودش خاموش می‌کند و فایل `/etc/pf.conf` شما دست‌نخورده می‌ماند.

استثناهای سطح سخت‌گیرانه (چیزهایی که همچنان از کارت فیزیکی می‌توانند خارج شوند): **IP ورودیِ خودِ سرورها** (وگرنه هسته نمی‌تواند به سرور برسد)، آدرس resolverهایی که باید bypass شوند، رنج‌های خصوصی/LAN، link-local، loopback، CGNAT، multicast، ساب‌نتِ خودِ تونل، و broadcast مربوط به DHCP (`255.255.255.255/32` — بستنش چند ساعت بعد lease دستگاه را می‌گیرد و هیچ‌کس این دو را به هم ربط نمی‌دهد).

**نکته‌های مهمِ سطح سخت‌گیرانه:**

- ⚠️ **با «دور زدن ایران» جمع نمی‌شود.** هرچه قانون‌های شما «مستقیم» بفرستند از کارت فیزیکی می‌رود، و این سطح دقیقاً همان را می‌بندد — یعنی سایت‌های ایرانی بالا نمی‌آیند. برنامه خودش وقتی این تناقض را ببیند هشدار می‌دهد: یا سطح را «استاندارد» کنید یا روتینگ را «گلوبال».
- در این سطح، resolverِ داخلیِ UDP هم از کانفیگ **حذف** می‌شود و فقط ورودی‌های DoH می‌مانند؛ وگرنه هر نام ایرانی باید timeout می‌شد. با geoip، «دور زدن ایران» همچنان کار می‌کند.
- روی بک‌اندِ **tun2socks** این سطح خیلی ضعیف‌تر از چیزی است که اسمش می‌گوید: `strict_route` ندارد، IPv6 اصلاً تونل نمی‌شود و قانون‌های فایروال هم فقط IPv4 هستند. برنامه هنگام اتصال این را در لاگ می‌گوید. برای گارد واقعی، **sing-box** را نصب کنید.
- کیل‌سوییچ و گارد به هم دست نمی‌زنند: قانون کیل‌سوییچ یک قانون `netsh` با **نام** است و قانون‌های گارد یک **گروه** فایروال‌اند، پس هیچ‌کدام دیگری را پاک نمی‌کند.
- اگر خودِ گارد شکست بخورد، **اتصال قطع نمی‌شود** — تونل بالا می‌ماند و برنامه جداگانه می‌گوید «تونل بالاست ولی کارت‌های فیزیکی DNS خودشان را نگه داشته‌اند».

**گارد در فاصله‌ی اتصال مجدد رها نمی‌شود** (از v1.2.0):

تا پیش از این، هر بازسازیِ تونل — تغییر شبکه، اعمال تنظیمات، عوض‌کردن سرور — **اول** گارد را رها می‌کرد و بعد می‌ساخت. در آن فاصله کارت TUN رفته و پروکسی خاموش است، پس همه‌چیز مستقیم از کارت فیزیکی می‌رود؛ override دی‌ان‌اس برداشته شده، پس **هر lookup به ISP می‌رسد**؛ و در سطح سخت‌گیرانه، کل گروه فایروال هم با آن رفته. کفِ این فاصله ۲ تا ۴ ثانیه است، واقعاً ۵ تا ۳۰ ثانیه در هر تلاش، و با احتساب چهار تلاشِ backoff **بیش از یک دقیقه**. قرار بود کیل‌سوییچ بپوشاندش، ولی کیل‌سوییچ پیش‌فرض خاموش است و فقط روی ویندوز کار می‌کند.

حالا گارد **نگه داشته می‌شود**: override دقیقاً سر جایش می‌ماند و فقط سوراخ‌های فایروال به اندازه‌ی سروری که قرار است شماره‌گیری شود باز می‌شوند؛ اتصال بعدی دوباره تنگشان می‌کند.

- **هزینه‌اش:** تا وقتی تونل پایین است نام‌ها resolve نمی‌شوند، چون کارت‌ها به peerی اشاره می‌کنند که جایی نمی‌رود. این شکستِ **بسته** است، نه باز — و پنهان نمی‌شود: اگر تلاش‌ها به نتیجه نرسند، یک بنر می‌گوید گارد هنوز نگه داشته شده و دکمه‌ی **«اینترنتم را برگردان»** می‌دهد.
- **رسیدِ نشست:** هر بار که گارد فعال می‌شود یک شناسه برمی‌گرداند. اتصالی که در رقابت با اتصال دیگری ببازد فقط با رسیدِ خودش می‌تواند رها کند — قبلاً بدون این، اتصالِ بازنده گاردِ برنده را روی تونلِ **زنده** برمی‌داشت و فایل وضعیت را پاک می‌کرد، یعنی موقع قطعِ واقعی دیگر چیزی برای بازگرداندن نبود.
- رها کردنِ بی‌قید دقیقاً همان‌جایی می‌ماند که باید: **قطع دستی، خروج، و exit**.

**اگر برنامه کرش کند یا برق برود:** فایل `userData/tun-state.json` (روی مک: `~/Library/Application Support/IRNetFree/tun-state.json`) **پیش از دست‌زدن به اولین کارت** نوشته می‌شود، پس حتی کرشِ وسطِ کار هم یک فهرست کاملِ «چه چیزی را باید برگرداند» جا می‌گذارد. اجرای بعدی خودکار برش می‌گرداند، پروسه‌های جامانده‌ی تونل را هم می‌کشد و در لاگ می‌نویسد چه کرد. روی مک این کار یک‌بار رمز می‌پرسد (کشتنِ پروسه و برگرداندنِ DNS در یک اسکریپت جمع شده‌اند تا فقط یک بار بپرسد).

### اتصال مجدد و تغییر شبکه

وقتی شبکه‌ی دستگاه زیر یک تونلِ زنده عوض می‌شود، هسته نمی‌میرد ولی سوکت‌هایش می‌شکنند: برنامه «متصل» می‌ماند و هیچ چیزی رد نمی‌شود. [`netWatcher.js`](src/main/netWatcher.js) این را می‌بیند و بازسازی می‌کند.

- **دکمه‌ی «اتصال مجدد»** روی صفحه‌ی خانه (فقط وقتی متصل هستید). همان مسیرِ بدون‌نشتیِ بالا را می‌رود؛ عمداً «قطع کن و دوباره وصل شو» **نیست**، چون آن کار دقیقاً همان پنجره‌ای را باز می‌کند که نگه‌داشتنِ گارد برای بستنش هست.
- **خودکار** هم سر جایش است و در تنظیمات قابل خاموش‌کردن.

**چه چیزی «تغییر شبکه» حساب می‌شود** (از v1.2.0 اصلاح شد):

- واچر می‌پرسد «آیا شبکه از **پایه جدا شد**؟»، نه «آیا الان با پایه فرق دارد؟». تفاوتش این است: لینکی که قطع شود و **با همان آدرس** برگردد — بلیپ Wi-Fi، ریست روتر، بیدارشدن روی همان شبکه‌ای که با آن خوابیده‌اید — با سؤال قبلی اصلاً دیده نمی‌شد، در حالی که همه‌ی سوکت‌های هسته با آن مرده بودند. این همان «وقتی نت می‌رود و می‌آید قاطی می‌کند» بود.
- آداپتورهای مجازیِ **بقیه‌ی نرم‌افزارها** (WSL / Docker / VMware / VirtualBox / Bluetooth) دیگر بازسازی راه نمی‌اندازند. سوییچِ External در Hyper-V عمداً استثناست، چون آدرس واقعیِ دستگاه را حمل می‌کند.
- **بی‌شبکه شدن** به‌تنهایی بازسازی راه نمی‌اندازد؛ چیزی که ماشه را می‌کشد **برگشتنِ** آدرس است. بازسازی روی «هیچ شبکه» فقط یک تلاشِ محکوم‌به‌شکست بود.
- بیدارشدن از sleep جداگانه هم از `powerMonitor` سیگنال می‌گیرد.

> یک محدودیتِ باقی‌مانده: اگر gateway عوض شود ولی آدرس‌ها دقیقاً یکی بمانند و لینک هم قطع نشود، `os.networkInterfaces()` چیزی نمی‌بیند. دیدنش به جدول مسیریابی نیاز دارد.

### بستن UDP در حالت پروکسی

قرینه‌ی گارد است برای وقتی که **TUN روشن نیست**: سوییچ **«بستن UDP در حالت پروکسی»** روی هر کارت شبکه‌ی فیزیکی UDPِ خروجی (به‌جز پورت ۵۳) را می‌بندد، تا WebRTC/STUN نتواند IP واقعی‌تان را لو بدهد — چون در حالت پروکسی، برنامه‌هایی که پروکسی سیستمی را نمی‌خوانند و همچنین UDP در هر صورت از کنار تونل رد می‌شوند. **بازی‌ها و تماس‌هایی که UDP لازم دارند از کار می‌افتند**، پس پیش‌فرض خاموش است. در حالت TUN این سوییچ غیرفعال نمایش داده می‌شود چون خودِ TUN، UDP را هم از تونل می‌برد.

- فقط روی **ویندوز** کار می‌کند (با همان گروه فایروالِ `IRNetFree`، پس با یک بار پاک‌سازی همراه گارد برداشته می‌شود).
- **کل شبکه‌ی محلی باز می‌ماند** — همان استثناهای سطح سخت‌گیرانه — تا DHCP، mDNS، SSDP و پرینتر شبکه سالم بمانند. قانونی که همه‌جا UDP را جز ۵۳ ببندد، چند ساعت بعد lease دستگاه را می‌گرفت.
- اگر سرورتان با **نام** آدرس‌دهی شده و ترنسپورتش UDP است، آدرسش پیش از ساختن قانون‌ها resolve و استثنا می‌شود، وگرنه اولین چیزی که قطع می‌شد خودِ سرور بود.
- شکستِ این کار هم اتصال را قطع نمی‌کند؛ فقط در لاگ می‌گوید «WebRTC هنوز می‌تواند آدرس شما را لو بدهد».

---

## 🧭 روتینگ (Routing)

تب **روتینگ** چهار سطح کنترل می‌دهد:

### ۱) روتینگ ساده (Simple)
یکی از حالت‌ها را انتخاب کنید:
- **گلوبال:** همه از پروکسی (به‌جز شبکه‌ی محلی).
- **دور زدن ایران:** ترافیک داخلی مستقیم، بقیه از پروکسی.
- **دور زدن چین:** مشابه، برای سایت‌های چین.
- **مستقیم:** همه‌چیز مستقیم (برای تست).
به‌علاوه: **بلاک تبلیغات** و **Sniffing** (تشخیص دامنه برای روتینگ دقیق‌تر).
> حالت‌های «دور زدن» و «بلاک تبلیغات» به فایل‌های `geoip.dat` و `geosite.dat` نیاز دارند (از بخش فایل‌های موردنیاز دانلود کنید).

### ۲) قوانین سفارشی (Custom rules)
هر خط به شکل `نوع , مقدار , خروجی`:
```
domain, geosite:google, proxy
ip, 8.8.8.8, direct
domain, geosite:category-ads-all, block
port, 443, proxy
```
خروجی‌ها: `proxy` / `direct` / `block`.

### ۳) روتینگ پیشرفته گرافیکی (Advanced)
سوییچ «روتینگ پیشرفته» را روشن کنید و قانون بسازید. هر قانون: **نوع → مقدار → مقصد**.

- **نوع:** `IP` / `دامنه` / `پورت` / `پروسه`.
- **مقدار:**
  - برای IP و دامنه یک **لیست پیشنهادی geoip/geosite** هست؛ لازم نیست حفظ باشید — تایپ کنید یا انتخاب کنید (مثل `geoip:ir`، `geosite:google`، `geosite:openai`).
  - مثال‌های دستی: `1.2.3.0/24`، `example.com`، `443`.
- **مقصد:** یک **منوی قابل‌جستجو** (مثل صفحه‌ی هوم) که همه‌ی کانفیگ‌ها، زنجیره‌ها، `مستقیم` و `بلاک` را نشان می‌دهد — برای لیست‌های طولانی، کافی است اسم را تایپ کنید.
- **بقیه‌ی ترافیک:** مقصد پیش‌فرض برای هر چیزی که با قانون‌ها مطابقت نکند.

قانون‌ها **از بالا به پایین** بررسی می‌شوند و **اولین تطابق** برنده است. سپس روی صفحه‌ی اتصال گزینه‌ی 🧭 را انتخاب و وصل شوید.

**مثال:** ترافیک اوپن‌ای‌آی از سرور آمریکا، سایت‌های ایرانی مستقیم، بقیه از سرور آلمان:
```
دامنه   geosite:openai     →  US-Server
دامنه   geosite:category-ir →  مستقیم
(بقیه‌ی ترافیک)            →  DE-Server
```

#### «حالت روتینگ ساده هم اعمال شود»
تیکِ کنارِ مقصدِ پیش‌فرض. وقتی روشن باشد، همان حالتی که بالای صفحه انتخاب کرده‌اید (دور زدن ایران / دور زدن چین) **زیرِ** قانون‌های خودتان هم اعمال می‌شود — پس دیگر لازم نیست بین «روتینگ پیشرفته» و «دور زدن ایران» یکی را انتخاب کنید یا قانون‌های `geosite:category-ir` را دستی بنویسید.

ترتیب دقیقِ قانون‌هایی که ساخته می‌شوند:

```
۰) قانون‌های DNS            ← همیشه اولِ همه (رهگیری پورت ۵۳ باید از bypassِ رنج خصوصی جلو بزند)
۱) بلاک تبلیغات            ← اگر روشن باشد و فایل‌های geo نصب باشند
۲) قانون‌های خودتان        ← رنج شرکت از وایرگارد می‌رود
۳) رنج‌های خصوصی/LAN       ← مستقیم
۴) حالت روتینگ ساده        ← فقط وقتی تیک روشن باشد (سایت‌های ایرانی → مستقیم)
۵) بقیه‌ی ترافیک           ← مقصد پیش‌فرضِ شما
```

- قانون‌های شما **قبل از** bypassِ رنج‌های خصوصی می‌آیند و این عمدی است: وگرنه یک دیتابیس روی رنج داخلی (مثل `10.20.0.0/16`) به‌جای رفتن از وایرگارد، «مستقیم» می‌شد.
- تیک، **فقط** بخشِ «این کشور مستقیم» از حالت ساده را اضافه می‌کند. «بقیه‌ی ترافیک» همیشه مالِ مقصد پیش‌فرضِ خودتان می‌ماند — هیچ‌وقت به `proxy` یا `direct`ِ حالت ساده تبدیل نمی‌شود.
- روی حالت **گلوبال** و حالت **مستقیم** چیزی اضافه نمی‌شود، چون کلِ محتوای آن دو همان «بقیه‌ی ترافیک» است که مقصد پیش‌فرض صاحبش است.
- DNS هم دنبال همین می‌رود: با تیکِ روشن و حالت «دور زدن ایران»، نام‌های ایرانی از resolver داخلی پرسیده می‌شوند.
- پیش‌فرض **خاموش** است، و چون جزو تنظیماتی است که در لحظه‌ی اتصال داخل کانفیگ پخته می‌شود، **با اتصال مجدد** اعمال می‌شود (برنامه خودش می‌پرسد).

> ⚠️ **اگر مقصد پیش‌فرض را یک وایرگاردِ split بگذارید** (وایرگاردی که `AllowedIPs` اش کل اینترنت نیست)، هر مقصدی جز همان رنج‌ها داخل تونل دور ریخته می‌شود و اینترنت عادی‌تان قطع می‌شود. برنامه همان‌جا هشدار می‌دهد؛ برای ترافیک عادی یک سرور دیگر را پیش‌فرض کنید.

#### کدهای geo هنگام ذخیره چک می‌شوند

یک کد اشتباه — مثل `geosite:ir` که **اصلاً وجود ندارد** (فهرست دامنه‌های ایران `geosite:category-ir` است) — باعث می‌شود هسته **کلِ کانفیگ** را رد کند، آن هم با پیامی که مثل «فایل geo نداری» به نظر می‌رسد:

```
infra/conf: code not found in geosite.dat: IR
```

به همین دلیل هنگام **ذخیره‌ی قانون‌ها**، برنامه کدهای geo را از خودِ هسته می‌پرسد (یک کانفیگ که فقط همان توکن‌ها را دارد به هسته داده می‌شود) و هر کدی را که در فایل‌های نصب‌شده نیست اسم می‌برد. اگر هسته یا فایل‌های geo نصب نباشند، چیزی به‌عنوان «خراب» گزارش نمی‌شود.

### ۴) روتینگ بر اساس پروسه (Process)
نوع قانون را روی **«پروسه»** بگذارید و از لیست برنامه‌های در حال اجرا یکی را انتخاب کنید (دکمه‌ی ⟳ برای تازه‌سازی لیست).

> ℹ️ **چطور کار می‌کند و محدودیت‌ها:** هسته‌ی Xray امکان تطبیق مستقیم «نام پروسه» را ندارد. بنابراین برنامه IPهای مقصدی را که آن پروسه به آن‌ها وصل می‌شود کشف کرده و آن‌ها را به مقصد انتخابی شما هدایت می‌کند (و در حافظه نگه می‌دارد). یعنی:
> - برای برنامه‌هایی با **سرور ثابت** (بازی، کلاینت اختصاصی، ابزار دیتابیس) بهترین نتیجه را دارد.
> - برنامه‌هایی که به IPهای مشترک CDN وصل می‌شوند (مثل مرورگر) قابل‌اعتماد نیست.
> - با روشن‌کردن «به‌روز نگه‌داشتن مسیر پروسه‌ها»، هر چند ثانیه مقصدهای جدید اضافه و هسته کوتاه ری‌لود می‌شود (پیش‌فرض خاموش = فقط هنگام اتصال). دکمه‌ی «پاک‌سازی IPهای یادگرفته‌شده» هم هست.

**مثال:** فقط ترافیک یک بازی (`game.exe`) از سرور با پینگ پایین، بقیه مستقیم.

---

## ⛓️ زنجیره پروکسی (Proxy Chains)

در تب **زنجیره** یک زنجیره بسازید: نام بدهید و چند سرور را **به‌ترتیب** بچینید (با کشیدن‌و‌رها کردن جابه‌جا می‌شوند). ترافیک از سرور اول → دوم → … → خروجی عبور می‌کند. هر زنجیره مثل یک «کانفیگ» در صفحه‌ی اتصال و در روتینگ پیشرفته قابل‌انتخاب است.

> 🔎 برای لیست بلند سرورها، بالای «سرورهای موجود» یک **کادر جستجو** هست و لیست **اسکرول‌دار** است.

**کاربرد رایج — دسترسی به دیتابیس از طریق WireGuard:** اگر WireGuard مستقیم وصل نمی‌شود، یک کانفیگ معمولی را **اول** و WireGuard را **آخر** زنجیره بگذارید؛ سپس در روتینگ پیشرفته رنج IP دیتابیس را به این زنجیره بفرستید:
```
IP   10.20.0.0/16   →  ⛓ DB-Chain   (Config → WireGuard)
```

---

## 🏢 وایرگارد شرکتی (Corporate WireGuard)

سناریوی خیلی رایج: شرکت یک فایل `.conf` وایرگارد می‌دهد که فقط **چند رنج داخلی** را می‌برد، یک **DNS داخلی** دارد که فقط او نام‌های شرکت را می‌شناسد، **دامنه‌ی جست‌وجو** دارد، و endpoint اش هم یک **نام** است نه IP. برنامه هر تکه‌ی این پازل را می‌شناسد.

### ۱) وارد کردن فایل

سه راه، هر سه یک نتیجه:

- دکمه‌ی **«انتخاب فایل .conf»** در فرم وایرگارد،
- **چسباندنِ متنِ کاملِ کانفیگ** (بلوکِ `[Interface]` + `[Peer]`) در «افزودن» — خودکار تشخیص داده می‌شود،
- یا پر کردن دستیِ فرم.

فقط **اولین** `[Peer]` استفاده می‌شود (کانفیگی با چند peer یک تنظیمات روتر است، نه کلاینت). کلیدها بی‌حساسیت به حروف بزرگ/کوچک خوانده می‌شوند و کامنت‌های `#`/`;` و خط‌های CRLF تحمل می‌شوند. اگر فیلدی کم باشد، پیام خطا **اسمِ همان فیلد** را می‌گوید.

> 💡 فیلد **Address** آدرس محلی خودتان است و حتماً `/32` (یا `/128` برای v6). اگر اشتباه ذخیره شده باشد (مثلاً کسی `192.168.x.0/16` گذاشته باشد) برنامه هنگام ساخت کانفیگ خودش اصلاحش می‌کند، وگرنه هسته اصلاً بالا نمی‌آمد.

### ۲) خطِ `DNS =` — هم resolver، هم دامنه‌ی جست‌وجو

`wg-quick` در یک خط هر دو را می‌گذارد:

```
DNS = 192.168.60.1, tes.systems
```

برنامه این را می‌شکند: **هر چه IP باشد resolver است، هر چه نام باشد دامنه‌ی جست‌وجو**. هر دو روی رکورد سرور ذخیره می‌شوند و در لینک اشتراکیِ خروجی هم می‌آیند. در فرم، همین را می‌توانید با کاما در فیلد **«DNS»** بنویسید.

### ۳) resolver از **داخلِ** همان تونل پرسیده می‌شود

نکته‌ی اصلی این‌جاست: DNS شرکت را فقط وقتی می‌شود پرسید که سؤال **از داخل همان تونل** برود. برنامه ([`src/main/dnsBuilder.js`](src/main/dnsBuilder.js)) این کار را می‌کند و ترتیبش هم حساب‌شده است:

| | چه کار می‌کند |
|---|---|
| **جای resolver شرکتی در فهرست** | **آخر**. یعنی resolver عمومی (DoH) هر چه بلد است جواب می‌دهد و فقط `NXDOMAIN` اش به DNS شرکت می‌افتد — پس نام‌های عمومی هرگز از داخل تونل به شرکت نمی‌روند. |
| **دامنه‌های جست‌وجو** | نام‌هایی که زیر آن دامنه‌اند **اول** به DNS شرکت داده می‌شوند — بدون رفت‌وبرگشت به resolver عمومی، و نام داخلیِ شما هیچ‌وقت بیرون دیده نمی‌شود. |
| **`AllowedIPs` → `expectedIPs`** | جوابی که در رنج‌های تونل نباشد دور ریخته می‌شود (تونل در هر صورت نمی‌توانست حملش کند). ورودی‌های `/0` حساب نمی‌شوند، و وقتی تنظیم **IPv6** خاموش است رنج‌های v6 هم کنار گذاشته می‌شوند — وگرنه هیچ جوابی از فیلتر رد نمی‌شد. |
| **مسیر خودِ سؤال** | یک قانون روتینگ اختصاصی، سؤال را از outbound همان مقصد بیرون می‌فرستد — نه «مستقیم»، نه از خروجیِ اصلی. |
| **زنجیره در برابر اتصال مستقیم** | اگر همان وایرگارد هم مستقیم و هم انتهای یک زنجیره مقصد باشد، **زنجیره برنده است** — چون اصلاً به‌خاطر همان endpointِ UDPِ بسته زنجیره ساخته شده. |

حداکثر **دو** resolver از هر سرور استفاده می‌شود.

### ۴) `AllowedIPs` تبدیل به پیشنهاد روتینگ می‌شود

وقتی در **روتینگ پیشرفته** مقصد یک قانون به یک وایرگارد ختم شود، همان‌جا رنج‌های `AllowedIPs` آن (بدون ورودی‌های `/0`) به‌عنوان **پیشنهاد** نشان داده می‌شوند و با یک دکمه در مقدارِ قانون می‌نشینند. اگر مقدار از قبل با آن‌ها یکی باشد، «اعمال شده» را می‌بینید.

سه حالت دیگر هم پیام مخصوص خودشان را دارند:

- **`AllowedIPs` کل اینترنت است** (`0.0.0.0/0`) → چیزی برای پیشنهاد نیست، رنج شرکت را خودتان بنویسید.
- **این وایرگارد را «پیش‌فرض» کرده‌اید** ولی split است → هشدار می‌دهد که هر مقصد دیگری داخل تونل دور ریخته می‌شود.
- **DNS دارد ولی دامنه‌ی جست‌وجو ندارد** → یادآوری می‌کند که با افزودن دامنه (مثل `tes.systems`) نام‌های داخلی مستقیم از همان DNS پرسیده می‌شوند.

### ۵) وقتی endpoint یک نام است

دو هسته این‌جا رفتار یکسانی ندارند:

- **هسته‌ی رسمی** نام را با DNS خودش (DoH، از داخل تونل) resolve می‌کند. دست‌نخورده می‌ماند.
- **فورک پترنیها** این کار را نمی‌کند: مستقیم که باشد از resolver سیستم‌عامل می‌پرسد و با `Unable to update bind: lookup <host>: no such host` می‌افتد؛ از داخل زنجیره هم نام خام را دست هاپ بعدی می‌دهد. نتیجه این است که یک وایرگاردِ شرکتیِ واردشده از `.conf` روی آن هسته **بی‌صدا هرگز بالا نمی‌آید** — همه‌ی مسیرهای دیگر کار می‌کنند و فقط تونل مرده است.

برنامه برای **همان یک هسته** نام را پیش از نوشتن کانفیگ به آدرس تبدیل می‌کند و در لاگ می‌نویسد (`WireGuard endpoint: host → ip`). نامی که resolve نشود دست‌نخورده می‌ماند تا خطای خودِ هسته گویاتر باشد.

### ۶) وقتی تونل جواب نمی‌دهد، خودش می‌گوید

یک وایرگارد که handshake اش کامل نمی‌شود، هر پنج ثانیه یک بسته می‌فرستد و **هیچ‌وقت جوابی نمی‌گیرد** — و هسته این را فقط در سطح لاگ `[Debug]` می‌نویسد که هیچ‌کس با آن اجرا نمی‌کند. برای کاربر فقط «سایت‌های شرکت باز نمی‌شوند» دیده می‌شود، در حالی‌که بقیه‌ی مسیرها سالم‌اند؛ و نتیجه‌گیریِ طبیعی این است که «روتینگ خراب است».

برنامه شمارنده‌های هر outbound را می‌بیند و اگر یکی مقدار قابل‌توجهی **فرستاده** و **هیچ چیز نگرفته** باشد، یک‌بار با صدای بلند می‌گوید:

> وایرگارد `<endpoint>`: ترافیک به این تونل فرستاده می‌شود ولی هیچ پاسخی برنمی‌گردد — یعنی handshake با peer کامل نمی‌شود. endpoint و پورتش را چک کن و این‌که هاپِ قبل از آن UDP را عبور می‌دهد یا نه.

سنجه‌اش عمداً محافظه‌کار است: تونلی که حتی یک بایت جواب بدهد، از نظر این هشدار سالم است.

### ۷) وایرگارد از داخل زنجیره

اگر endpointِ UDPِ وایرگارد بسته باشد، یک کانفیگ معمولی را **اول** و وایرگارد را **آخر** زنجیره بگذارید ([زنجیره پروکسی](#-زنجیره-پروکسی-proxy-chains)). در این حالت برنامه خودکار `bufferSize` را روی صفر می‌گذارد؛ بدون آن، تبدیل UDP↔TCP بسته‌ها را خراب می‌کند (`unknown type packet`) و تونل **بی‌صدا** هیچ داده‌ای رد نمی‌کند.

> 🔧 جزئیات داخلی: در کانفیگی که به هسته داده می‌شود `allowedIPs` هر peer روی `0.0.0.0/0, ::/0` باز می‌شود. `allowedIPs` فایروال نیست — فقط می‌گوید این outbound *اجازه دارد* چه چیزی حمل کند؛ این‌که چه چیزی به آن **می‌رسد** را قانون‌های روتینگ تعیین می‌کنند که از روی همان رنج‌ها ساخته شده‌اند. رکورد ذخیره‌شده رنج‌های اصلی را نگه می‌دارد، پس پیشنهادهای روتینگ و `expectedIPs` دست‌نخورده می‌مانند.

**می‌خواهید مطمئن شوید این مسیر واقعاً کار می‌کند؟** `npm run probe:wg` همین سناریو را سرتاسری اجرا می‌کند — [آمار و تست](#-آمار-و-تست-stats--testing).

---

## 🧩 استخر پروکسی (Multi-Proxy Pool)

گاهی می‌خواهید **چند خروجی هم‌زمان** داشته باشید — مثلاً یک برنامه از سرور آلمان، یکی از سرور آمریکا و یکی از یک زنجیره — هرکدام روی **پورت محلی جدا**. تب **استخر پروکسی** دقیقاً همین کار را می‌کند:

- روی **«+ پروکسی جدید»** بزنید تا یک ردیف اضافه شود؛ برای هر ردیف:
  - **خروجی:** یک کانفیگ یا زنجیره (منوی قابل‌جستجو).
  - **پورت SOCKS:** پورت محلی این خروجی (خودکار از `60001` به بالا پیشنهاد می‌شود).
  - **پورت HTTP:** اختیاری.
  - سوییچ **فعال/غیرفعال**.
- سپس از دکمه‌ی **«اتصال استخر»** یا از صفحه‌ی اتصال گزینه‌ی 🧩 وصل شوید. یک نمونه‌ی Xray بالا می‌آید و **همه‌ی پورت‌ها هم‌زمان** فعال می‌شوند.
- در برنامه‌های خود، هرکدام را به پورت دلخواه وصل کنید:
  ```
  App A → SOCKS 127.0.0.1:60001   (خروجی: DE‑Server)
  App B → SOCKS 127.0.0.1:60002   (خروجی: US‑Chain)
  ```

**سازگاری با پروکسی و TUN:** پورت‌های استاندارد (SOCKS/HTTP تنظیمات) هم باز می‌شوند و به **«اولین پروکسیِ فعالِ استخر»** (به‌عنوان خروجی اصلی) وصل‌اند؛ بنابراین **پروکسی سیستمی، حالت TUN و بررسی IP** دقیقاً مثل حالت تک‌کانفیگ کار می‌کنند و پورت‌های استخر خروجی‌های *اضافه* روی آن هستند. در حالت TUN، IP سرورِ همه‌ی خروجی‌های استخر برای جلوگیری از حلقه، bypass می‌شوند.

> ⚠️ پورت‌ها نباید تکراری باشند (برنامه هنگام تخصیص خودکار این را رعایت می‌کند). اگر یک پورت اشغال باشد، Xray هنگام اتصال خطای واضح می‌دهد. **پورت آمار** (پیش‌فرض `10085`) هم از قبل رزرو است و یک ردیف استخر نمی‌تواند آن را بگیرد.

> ℹ️ استخر خودش قانون «دور زدن کشور» تولید نمی‌کند — هر ردیف، خروجیِ خودش را دارد و DNS هم مثل حالت «گلوبال» رفتار می‌کند. اگر resolver داخلیِ ایران این‌جا فعال می‌شد، فقط یک IP ایرانی دستِ خروجیِ خارج از کشور می‌داد.

---

## 🧩 DNS

برنامه دو فهرست DNS دارد (هر دو در تنظیمات، با کاما یا از منوی آماده):

- **DNS خارجی** — برای همه‌ی دنیا، از داخل تونل. پیش‌فرض DoH است (`https://1.1.1.1/dns-query, https://8.8.8.8/dns-query`) چون سرورهایی که UDP را می‌بندند دیگر DNS را نمی‌شکنند. منوی آماده: Cloudflare / Google / Quad9 / AdGuard (DoH) و نسخه‌ی UDP آن‌ها. `host:port` هم پذیرفته می‌شود.
- **DNS داخلی** — فقط در حالت «دور زدن ایران» (یا قانون پیشرفته‌ی `geosite:category-ir → مستقیم`)، **و فقط وقتی فایل‌های geo نصب باشند**: دامنه‌های ایرانی از این‌جا و **مستقیم** resolve می‌شوند و جواب باید IP ایرانی باشد. پیش‌فرض شکن (`178.22.122.100, 185.51.200.2`)؛ منوی آماده: شکن / الکترو / بگذر / 403.
- **مدیریت DNS توسط برنامه** (پیش‌فرض روشن) — هر بسته‌ی DNS که به هسته برسد همان‌جا جواب داده می‌شود؛ در حالت TUN، DNS سیستم روی آدرس تونل قرار می‌گیرد و از تونل می‌رود. خاموش = رفتار قدیمی (فهرست خارجی همان‌طور که هست، بدون رهگیری).
- **IPv6** (پیش‌فرض خاموش) — خاموش یعنی هسته فقط IPv4 برمی‌گرداند تا برنامه‌ها سراغ آدرسی نروند که از تونل عبور نمی‌کند.
- **DNS ای که به یک مقصد تعلق دارد** — یک وایرگارد شرکتی که در کانفیگش DNS دارد، نام‌هایی را می‌شناسد که هیچ‌کس دیگر نمی‌شناسد؛ ولی فقط وقتی که سؤال **از داخل همان تونل** برود. برنامه چنین resolverهایی را **آخرِ** فهرست می‌گذارد و سؤالشان را از outbound خودِ همان مقصد بیرون می‌فرستد → [وایرگارد شرکتی](#-وایرگارد-شرکتی-corporate-wireguard).

**چند رفتار دیگر که خوب است بدانید:**

- پرس‌وجوهای غیر از A و AAAA (مثل PTR/SRV/TXT/HTTPS) با `REFUSED` جواب داده می‌شوند، نه این‌که به مقصد اصلی‌شان فرستاده شوند — در حالت TUN آن مقصد خودِ peerِ تونل است و سؤال داخل حلقه می‌افتاد.
- resolver داخلی `expectedIPs` دارد: جوابی که IP ایران (یا چین) نباشد دور ریخته می‌شود، پس جوابِ مسموم قبول نمی‌شود. این به `geoip.dat` نیاز دارد.
- resolver داخلی هیچ‌وقت درباره‌ی بقیه‌ی دنیا پرسیده نمی‌شود (fallback نمی‌شود)، ولی resolverِ یک مقصد **می‌شود** — او باید برای نام‌هایی که هیچ‌کس نمی‌شناسد باقی بماند.
- resolverی که روی یک رنج خصوصی باشد (مودم، DNS داخلیِ اداره) خودکار از تونل مستثنی می‌شود، وگرنه سؤال به جایی نمی‌رسید.
- در گارد **سخت‌گیرانه**، ورودی‌های UDPِ فهرست داخلی از کانفیگ حذف می‌شوند و فقط DoH می‌ماند (چون UDPِ بیرون از تونل بسته است) — به‌جای timeout روی هر نام ایرانی، بلافاصله می‌افتد روی DoH.
- **بدون فایل‌های geo، هیچ resolver داخلی‌ای ساخته نمی‌شود.** این یک نشتی واقعی بود که در v1.2.0 بسته شد: بدون `geoip.dat`/`geosite.dat`، «دور زدن ایران» عملاً روتینگ گلوبال است — روتر هیچ قانون کشوری تولید نمی‌کند و هر بایت از پروکسی می‌رود — ولی نقشه‌ی resolver همچنان سرور داخلی را می‌ساخت. نتیجه این‌که `snapp.ir` و `bmi.ir` به یک resolver ایرانی می‌رفتند، **UDP رمزنشده و بیرون از تونل**، در حالی که خودِ اتصال proxy می‌شد: DNS دقیقاً همان چیزی را لو می‌داد که ترافیک پنهانش کرده بود، و هیچ سودی هم نداشت (جواب‌های ضدتحریمِ شکن وقتی اتصال proxy است بی‌فایده‌اند). چون یک نصب تازه هنوز فایل‌های geo را دانلود نکرده، این حالتِ پیش‌فرضِ همان نصب بود.
- **peerِ تونل روی هر دو خانواده به کارت‌ها داده می‌شود، مستقل از تنظیم IPv6.** قبلاً فقط وقتی داده می‌شد که IPv6 روشن بود؛ با خاموش بودنش هر کارت فیزیکی resolverهای IPv6 خودِ ISP را نگه می‌داشت — و آن‌ها on-link هستند، یعنی هرگز به مسیر پیش‌فرضِ تونل نمی‌رسند و برای هر برنامه‌ی v6-capable نشت می‌کردند.
- فهرستِ آدرس‌هایی که باید از تونل مستثنی شوند (تا resolver در دسترس بماند) از **کانفیگی که واقعاً اجرا می‌شود** خوانده می‌شود، نه از نو ساخته می‌شود؛ وگرنه می‌توانست resolverی را نام ببرد که کانفیگ ندارد یا یکی را که دارد جا بیندازد.

فهرست قدیمی `DNS` هنگام اجرای اول به این دو فهرست تبدیل می‌شود (IPهای عمومی شناخته‌شده به DoH، DNS های ایرانی به فهرست داخلی).

---

## 🌐 اشتراک در شبکه محلی (LAN)

سوییچ «اجازه به شبکه محلی» را روشن کنید تا این دستگاه به‌عنوان پروکسی برای بقیه‌ی دستگاه‌های شبکه عمل کند.

- به‌محض روشن‌کردن، **آدرس و پورت** نمایش داده می‌شود، مثل:
  `آدرس برای دستگاه‌های دیگر: 192.168.1.20:10809 (HTTP) • 192.168.1.20:10808 (SOCKS)`
- روی دستگاه دیگر، همان IP و پورت را به‌عنوان پروکسی HTTP یا SOCKS وارد کنید.
- روی ویندوز برای باز شدن **فایروال** به **دسترسی ادمین** نیاز است (برنامه خودش پورت‌ها را باز می‌کند).
- پس از تغییر این گزینه، یک‌بار دوباره وصل شوید تا اعمال شود.

---

## 🚨 کیل‌سوییچ (Kill Switch)

سوییچ «کیل‌سوییچ» را روشن کنید تا اگر اتصال **به‌طور ناگهانی** قطع شد، **کل اینترنت بسته شود** و هیچ ترافیکی بدون تونل نشت نکند.

- هنگام قطع ناگهانی، یک **بنر قرمز** ظاهر می‌شود با دو دکمه:
  - **اتصال مجدد** — دوباره وصل می‌شود.
  - **باز کردن اینترنت** — مسدودسازی را برمی‌دارد و به حالت عادی برمی‌گردد.
- قطع **عمدی** توسط خودتان کیل‌سوییچ را فعال نمی‌کند.
- روی ویندوز به **دسترسی ادمین** نیاز دارد (با فایروال کار می‌کند). اگر برنامه بسته یا کرش شود، قانون فایروال خودکار پاک می‌شود تا اینترنت دائمی بسته نماند.

---

## 🔐 اجرای با دسترسی ادمین (Run as Admin)

حالت TUN، کیل‌سوییچ و باز کردن فایروال LAN روی ویندوز نیازمند دسترسی ادمین‌اند. سه راه:
1. ترمینال/برنامه را **Run as administrator** اجرا کنید، سپس `npm start`.
2. از داخل برنامه دکمه/پیام **«اجرا با دسترسی ادمین»** را بزنید (خودش بسته و دوباره با ادمین باز می‌شود).
3. (برای تست توسعه، بدون ساخت exe) از PowerShell:
   ```powershell
   Start-Process ".\node_modules\electron\dist\electron.exe" -ArgumentList "<مسیر-کامل-پروژه>" -Verb RunAs
   ```

---

## 🍎 اولین اجرا روی مک (macOS — First Run)

> ⚠️ نیمه‌ی macOSِ حالت TUN، گارد نشتی و آماده‌سازی باینری‌ها **بدون دسترسی به یک مک واقعی** نوشته شده است. اگر چیزی کار نکرد، پیش از هر کار دیگری [اسکریپت خودآزما](#۵-اسکریپت-خودآزما-self-check) را اجرا کنید و **کل خروجی‌اش** را در ایشو بگذارید — سریع‌ترین راه رفع مشکل همین است.

### ۱) Gatekeeper — اولین باز کردن برنامه

برنامه با گواهی Apple امضا نشده، پس دابل‌کلیک ساده ممکن است با پیام «…can't be opened» رد شود. **فقط یک‌بار**:

- روی `IRNetFree.app` **راست‌کلیک → Open** بزنید و در دیالوگ روی **Open** تأیید کنید،
- یا در ترمینال:
  ```bash
  xattr -cr /Applications/IRNetFree.app
  ```

از دفعه‌ی بعد مثل هر برنامه‌ی دیگری باز می‌شود.

### ۲) دانلود هسته‌ها

تنظیمات → **فایل‌های موردنیاز** → `هسته Xray` و `هسته sing-box`. برنامه خودش معماری درست (Intel یا Apple Silicon) را می‌گیرد، صفت **quarantine** را از باینری پاک می‌کند و آن را **ad‑hoc امضا** می‌کند؛ روی Apple Silicon بدون این امضا باینری موقع اجرا با `Killed: 9` بسته می‌شود و خطایش هیچ ربطی به علت واقعی ندارد. اگر امضا شکست خورد، یک‌بار ابزارهای خط فرمان Xcode را نصب کنید و دوباره دانلود بزنید:

```bash
xcode-select --install
```

### ۳) اولین اتصال TUN — پنجره‌ی رمز عبور

حالت TUN روی مک به دسترسی root نیاز دارد، ولی **کل برنامه** با root اجرا نمی‌شود؛ فقط همان چند دستور در لحظه‌ی اتصال بالا می‌روند. پس ممکن است هنگام اتصال **تا دو پنجره‌ی رمز** ببینید:

1. یکی برای بالا آوردن خودِ تونل (`sing-box` به‌عنوان root)؛
2. و اگر **گارد نشتی** روی «استاندارد» باشد، یکی برای نوشتن DNS روی همه‌ی سرویس‌های شبکه.

اگر پنجره را Cancel کنید، اتصال با پیام «برای حالت TUN باید اجازه دسترسی (رمز عبور) بدهید» متوقف می‌شود — این یعنی همه‌چیز درست کار می‌کند، فقط اجازه ندادید.

> اگر برنامه **بد بسته شود** (کرش، قطع برق، Force Quit)، اجرای بعدی یک‌بار رمز می‌پرسد تا DNS اصلی را برگرداند و تونلِ جامانده را ببندد. در لاگ می‌نویسد چه کرد. این طبیعی است.

### ۴) چه چیزی روی مک آزمایشی است

| بخش | وضعیت |
|---|---|
| **TUN با sing-box** | مسیر اصلی. روت‌ها را خود sing-box با `auto_route` می‌گذارد و نام دستگاه را کرنل می‌دهد (`utun0`، `utun3`، …) — برنامه دستگاهِ تازه‌ظاهرشده را پیدا می‌کند و در لاگ می‌نویسد. |
| **گارد استاندارد** | با `networksetup -setdnsservers` روی همه‌ی سرویس‌های شبکه. مقدارهای اصلی پیش از تغییر در `tun-state.json` ذخیره می‌شوند. |
| **گارد سخت‌گیرانه** | **آزمایشی** — با یک anchor از `pf`؛ هنوز روی مک واقعی آزمایش نشده. خودِ تنظیمات هم این را می‌گوید. اگر شبکه‌تان قطع شد، گارد را روی «استاندارد» برگردانید. |
| **tun2socks** (بک‌اند قدیمی) | کار می‌کند ولی فقط IPv4 است؛ فقط وقتی سراغش بروید که sing-box مشکل داشته باشد. |

### ۵) اسکریپت خودآزما (Self-check)

```bash
npm run selfcheck:mac

# یا مستقیم، از پوشه‌ی پروژه:
bash scripts/mac-selfcheck.sh

# اگر می‌خواهید نسخه‌ی نصب‌شده را هم چک کند، مسیر .app را بدهید:
bash scripts/mac-selfcheck.sh /Applications/IRNetFree.app

# برای دیدن وضعیت فایروال pf (فقط این بخش به root نیاز دارد):
sudo bash scripts/mac-selfcheck.sh
```

اسکریپت **فقط می‌خواند**: هیچ تنظیمی را عوض نمی‌کند، هیچ پروسه‌ای را نمی‌کشد و هیچ تونلی بالا نمی‌آورد (تنها چیزی که می‌نویسد یک کانفیگ موقت برای `sing-box check` است که خودش پاکش می‌کند). هر بررسی **یک خط** چاپ می‌کند با برچسب `OK` / `WARN` / `FAIL`:

| بخش | چه چیزی را می‌بیند |
|---|---|
| **باینری‌ها** | وجود و اجراپذیریِ `xray`، `xray-pattn` و `sing-box` در پوشه‌ی داده‌ها (و داخل `.app` اگر مسیرش را بدهید)، وضعیت `codesign`، صفت quarantine، و خروجی `version` هرکدام |
| **شبکه** | دروازه و اینترفیس پیش‌فرض، نام سرویسِ `networksetup` آن اینترفیس، DNS فعلی‌اش، فهرست دستگاه‌های `utun`، و وضعیت `pf` |
| **کانفیگ TUN** | همان کانفیگی که برنامه می‌سازد را با `sing-box check` اعتبارسنجی می‌کند |
| **باقی‌مانده‌ها** | `tun-state.json`، پوشه‌های موقت `irnf-*`، و پروسه‌های `sing-box`/`tun2socks` که هنوز در حال اجرا هستند |

نمونه‌ی خروجی:

```
-- network --
OK    default route: gateway 192.168.1.1 via en0
OK    network service for en0: Wi-Fi
OK    DNS of Wi-Fi: automatic (DHCP)
OK    utun devices: utun0 utun1
WARN  pfctl needs root — re-run with: sudo bash scripts/mac-selfcheck.sh

-- summary --
OK 18 · WARN 2 · FAIL 0
```

> 📋 **هنگام گزارش مشکل، کل خروجی را از خط اول تا `summary` بچسبانید** — نه فقط خط‌های `FAIL`. خط‌های `OK` هم بخشی از تشخیص‌اند.

### ۶) چک‌لیست تأیید روی مک (Verification checklist)

بعد از اولین نصب، این‌ها را به‌ترتیب بزنید؛ هرکدام یک خط از طرحِ تأیید فاز ۳ است:

| ✔ | آزمون | انتظار |
|---|---|---|
| ☐ | `npm run selfcheck:mac` | هیچ خط `FAIL` ای نباشد |
| ☐ | اتصال در **حالت پروکسی** (بدون TUN) → «بررسی IP» | IP و کشورِ سرور |
| ☐ | **حالت TUN** با **بک‌اندِ TUN** = `sing-box` | وصل می‌شود؛ در لاگ `TUN device: utunN` و «حالت TUN فعال شد» |
| ☐ | زیر TUN، در ترمینال: `route -n get default` | خط `interface:` روی `utunN` باشد، نه `en0` |
| ☐ | زیر TUN: [ipleak.net](https://ipleak.net) | نه DNS مودم/ISP دیده شود، نه IPv6 (وقتی تنظیم **IPv6** خاموش است) |
| ☐ | **گارد نشتی** = `استاندارد` → `networksetup -getdnsservers Wi-Fi` | `172.19.0.2` (آدرس تونل) |
| ☐ | **گارد نشتی** = `سخت‌گیرانه` (آزمایشی) → [browserleaks.com/webrtc](https://browserleaks.com/webrtc) | فقط IP خروجی؛ نشانی محلی/واقعی نه |
| ☐ | زیر TUN **سرور را عوض کنید** | بعد از چند ثانیه مرور دوباره کار می‌کند (بدون قطع دستی) |
| ☐ | **قطع اتصال** → دوباره `networksetup -getdnsservers Wi-Fi` | مقدار اصلی برگشته (یا «There aren't any DNS Servers set…») |
| ☐ | بعد از قطع: `pgrep -fl sing-box` و `ifconfig -l` | نه پروسه‌ای مانده، نه `utun`ِ ما |
| ☐ | (اختیاری) **کرش‌تست**: برنامه را Force Quit کنید و دوباره بازش کنید | یک‌بار رمز می‌پرسد، DNS خودکار برمی‌گردد و در لاگ می‌نویسد |
| ☐ | مصرف CPU در حالت بی‌کار (Activity Monitor) | `IRNetFree` و هسته‌ها روی‌هم ≤ ۱٪ |

---

## 📦 فایل‌های موردنیاز (Required Files)

در **تنظیمات → فایل‌های موردنیاز**، هر مؤلفه با یک کلیک دانلود/به‌روزرسانی می‌شود (بدون ساخت دوباره‌ی برنامه):

| مؤلفه | کاربرد |
|---|---|
| **هسته Xray** | اجرای اصلی پروکسی/تونل (نسخه‌اش هم نمایش داده می‌شود) |
| **هسته Xray-PattN** | fork پترنیها، ساخته‌شده از شاخه‌ی `main` بالادست؛ کانفیگ‌های VLESS/Trojan بدون TLS (مثل ws روی پورت ۸۰ پشت CDN) و فرگمنترِ `finalmask` را می‌پذیرد. ریلیز فعلی هسته‌ی رسمی هم کانفیگ‌های بدون TLS را اجرا می‌کند، ولی هر وقت بالادست چکِ شاخه‌ی `main` را منتشر کند، برنامه خودکار روی این هسته اجرایشان می‌کند → [دو هسته‌ی Xray](#-دو-هستهی-xray-cores) |
| **هسته sing-box** | بک‌اندِ پیش‌فرضِ **حالت TUN** (و ضد DPI) |
| **tun2socks** | حالت TUN — بک‌اندِ **قدیمی**، جایگزین وقتی sing-box نصب نیست |
| **wintun.dll** | حالت TUN (فقط ویندوز) — هر دو بک‌اند به آن نیاز دارند |
| **geoip + geosite** | روتینگ مبتنی بر کشور/دسته (دور زدن ایران/چین، بلاک تبلیغات، قوانین `geoip:`/`geosite:`) |

- برنامه به‌طور خودکار معماری درست (x64/arm64) را می‌گیرد و روی مک باینری‌ها را برای اجرا روی Apple Silicon امضای ad‑hoc می‌کند.
- دکمه‌ی **«حذف فایل‌های دانلودشده»** فایل‌های دانلودشده را از پوشه‌ی داده‌ها پاک می‌کند: هر دو هسته‌ی Xray، `tun2socks`، `wintun.dll` و فایل‌های geo. نسخه‌ی همراهِ نصب و هسته‌ای که خودتان مسیرش را داده‌اید دست‌نخورده می‌مانند؛ باینری `sing-box` هم پاک می‌شود. پیش از این کار باید **قطع** باشید.
- **به‌روزرسانی خودکار فایل‌ها:** هفته‌ای یک بار و فقط وقتی وصل نیستید. پیش‌فرض فقط فایل‌های geo را تازه می‌کند — داده‌اند و کانفیگی را خراب نمی‌کنند. گزینه‌ی «geo و هسته‌ها» هسته‌های نصب‌شده را هم وقتی ریلیز جدیدتری باشد عوض می‌کند؛ چون هسته‌ی جدید می‌تواند رفتار عوض کند، این یکی فقط با انتخاب خودتان است.

**لایسنس مؤلفه‌های همراه (Bundled third-party binaries)** — باینری‌هایی که در `bin/` کامیت شده‌اند و با نصب‌کننده توزیع می‌شوند، متن لایسنس خودشان را کنارشان دارند:

| باینری | لایسنس | متن |
|---|---|---|
| [tun2socks](https://github.com/xjasonlyu/tun2socks) v2.6.0 | MIT | `bin/LICENSE-tun2socks.txt` |
| [Wintun](https://www.wintun.net/) | — | `bin/LICENSE-wintun.txt` |

هسته‌ی Xray، sing-box و فایل‌های geo در ریپو کامیت نشده‌اند و هنگام اجرا دانلود می‌شوند، پس لایسنس‌شان همراه خودشان می‌آید.

---

## 🆙 نسخه و به‌روزرسانی (Version & Update)

در **تنظیمات → درباره و به‌روزرسانی**:
- **نسخه‌ی برنامه** نمایش داده می‌شود.
- دکمه‌ی **«بررسی به‌روزرسانی»** نسخه‌ی فعلی را با آخرین ریلیز گیت‌هاب مقایسه می‌کند؛ اگر نسخه‌ی جدیدی باشد، دکمه‌ی **«دانلود»** نصب‌کننده‌ی همین سیستم‌عامل را داخل برنامه دانلود می‌کند، SHA-256 اش را با فایل‌های `SHA256SUMS-*.txt` که ریلیز کنارش منتشر می‌کند مقایسه می‌کند و فقط اگر مطابق بود بازش می‌کند. مطابق نبود → فایل پاک می‌شود و می‌گوید. checksum ای منتشر نشده بود → فایل فقط در پوشه‌اش نشان داده می‌شود و اجرا نمی‌شود. اگر برای این سیستم‌عامل نصب‌کننده‌ای نباشد، مثل قبل صفحه‌ی ریلیز باز می‌شود.
- **پشتیبان‌گیری (JSON)** همه‌ی سرورها، ساب‌ها، زنجیره‌ها، استخر، تنظیمات و مصرف را در یک فایل می‌گذارد؛ **بازیابی از فایل** ادغام بر اساس شناسه است — آنچه روی این دستگاه هست می‌ماند، فقط موارد تازه اضافه می‌شوند، و دوبار بازیابی‌کردن با یک بار فرقی ندارد. فایلی که مال این برنامه نباشد رد می‌شود.
- **نسخه‌ی هسته‌ی Xray** هم در همین صفحه و کنار «فایل‌های موردنیاز» نشان داده می‌شود.

---

## 📊 آمار و تست (Stats & Testing)

- **پینگ TCP:** زمان handshake تا سرور (سریع).
- **تأخیر دانلود:** یک نمونه‌ی موقتی Xray بالا می‌آید و تأخیر واقعی از داخل تونل اندازه گرفته می‌شود (برای زنجیره هم کار می‌کند) — یعنی مدرکِ این‌که کانفیگ واقعاً کار می‌کند، نه فقط پورتش باز است.
- **تأخیر آپلود:** چند صد کیلوبایت از داخل همان تونل فرستاده می‌شود؛ کانفیگ‌هایی را لو می‌دهد که دانلودشان خوب است ولی آپلودشان کند/خراب است.
- **بررسی IP:** IP و کشور خروجی (مستقیم یا از طریق پروکسی).
- **آمار زنده:** سرعت و حجم دانلود/آپلود لحظه‌ای + مجموع این نشست، و یک **نمودار شصت‌ثانیه‌ای سرعت** (دو خط، یک canvas، بدون DOM در هر تیک). (از `metrics` روی HTTP خوانده می‌شود و **هر outbound** را جدا گزارش می‌کند — به همین دلیل آمار در حالت استخر و روتینگ پیشرفته هم درست است.)
- **تست همه:** پینگ هم‌زمان همه‌ی سرورها/زنجیره‌ها. تأخیر واقعیِ همه‌شان با **یک** هسته‌ی موقت گرفته می‌شود (هر هدف روی ورودی SOCKS خودش)، نه یکی برای هر سرور.

### مصرف کل هر کانفیگ

شمارنده‌های هسته با هر بار بالا آمدنش از صفر شروع می‌شوند، پس تنها چیزی که می‌شد گفت «این نشست چقدر رفت» بود. حالا اختلاف شمارنده‌ها به مجموع دائمیِ همان کانفیگ اضافه می‌شود — با این قاعده که شمارنده‌ای که **کم** شده یعنی هسته دوباره بالا آمده و خودِ عدد جدید همان اختلاف است، پس نه بایتی در ری‌استارت گم می‌شود و نه دوبار شمرده می‌شود. مجموع‌ها در `usage.json` (کنار `store.json`) ذخیره می‌شوند و با حذف کانفیگ پاک می‌شوند؛ زنجیره زیر شناسه‌ی خودش شمرده می‌شود نه زیر آخرین هاپش.

پاک‌کردنش دست خودتان است: کلیک روی عددِ مصرف روی کارت هر کانفیگ آن یکی را پاک می‌کند، و دکمه‌ی **«پاک‌کردن مصرف»** بالای صفحه‌ی سرورها همه را. خودِ کانفیگ‌ها دست نمی‌خورند.

### تست‌های توسعه‌دهنده

```bash
npm test            # تست‌های واحد (بدون نیاز به هسته)
npm run validate    # هر شکلِ کانفیگی که ساخته می‌شود را به هسته می‌دهد: `xray run -test`
npm run probe:wg    # سناریوی وایرگارد شرکتی، سرتاسری، با یک peer واقعی
node scripts/probe-dns-leak.js   # هر مسیرِ DNS را دنبال می‌کند و می‌گوید کجا رفت
```

سه لایه‌ی جداگانه‌اند و هر کدام چیز دیگری ثابت می‌کند:

| دستور | چه چیزی را ثابت می‌کند | به چه نیاز دارد |
|---|---|---|
| `npm test` | خروجیِ خودمان همان است که باید باشد (شکلِ کانفیگ، پارسر، متنِ اسکریپت‌های گارد، …) | هیچ |
| `npm run validate` | **هسته کانفیگ را قبول می‌کند** — هر ترکیبی از نقشه × حالت DNS × حالت روتینگ × بود/نبودِ فایل geo × IPv6 ساخته و با `xray run -test` سنجیده می‌شود | `bin/xray` |
| `npm run probe:wg` | **واقعاً کار می‌کند** — سناریوی وایرگارد شرکتی، سرتاسری | `bin/xray` + یک sing-box |
| `probe-dns-leak.js` | **هیچ پرس‌وجویی فرار نمی‌کند** — هر مسیر DNS دنبال و نسبت داده می‌شود | `bin/xray` |

#### `npm run validate`

```bash
npm run validate
IRNF_XRAY_EXE=/path/to/xray-pattn npm run validate    # همان شکل‌ها روی هسته‌ی دوم
IRNF_SINGBOX_EXE=/path/to/sing-box npm run validate   # کانفیگ‌های TUN هم با `sing-box check` سنجیده می‌شوند
```

#### `npm run probe:wg` — وایرگارد شرکتی، سرتاسری

[`scripts/probe-wg-chain.js`](scripts/probe-wg-chain.js) یک **peer واقعی وایرگارد** (با sing-box، با رمزنگاری واقعی) بالا می‌آورد، یک **DNS شرکتی** و یک **سایت داخلی** پشتش می‌گذارد، یک هاپ VLESS جلویش قرار می‌دهد، و کانفیگِ کلاینت را با **همان `buildConfig()`ِ خودِ برنامه** می‌سازد:

```
هسته‌ی کلاینت ──vless──▶ هاپ (xray) ──udp──▶ peer وایرگارد (sing-box)
      │                                              │
      └── socks 127.0.0.1:PORT                       └──▶ DNS شرکت + سایت داخلی
```

بعد به‌ترتیب چک می‌کند: پورت SOCKS بالا آمد → صفحه‌ی داخلی با `200` برگشت → **واقعاً از DNS شرکت پرسیده شد** (نه از جای دیگر) → ۲ مگابایت روی HTTP → یک TLS handshake واقعی از داخل تونل → ۲ مگابایت روی HTTPS. **هر هسته‌ی فرمتِ Xray که پیدا کند را از همین مسیر رد می‌کند**، تا اختلاف رفتار دو هسته این‌جا معلوم شود نه در گزارش کاربر.

همه‌چیز روی loopback است: به مسیرها، آداپتورها و فایروال دست نمی‌زند.

```bash
IRNF_SINGBOX_EXE=/path/to/sing-box IRNF_XRAY_EXE=/path/to/xray-pattn npm run probe:wg
```

| متغیر محیطی | کار |
|---|---|
| `IRNF_SINGBOX_EXE` | **اجباری** — باینری‌ای که نقش peer وایرگارد را بازی می‌کند |
| `IRNF_XRAY_EXE` | هسته‌ی اضافه‌ای که هم تست شود (مثلاً فورک patterniha) |
| `IRNF_PROBE_PORT` | پورت پایه، پیش‌فرض `39700` (بقیه‌ی پورت‌ها از رویش ساخته می‌شوند) |
| `IRNF_PROBE_TLS=1` | هاپ روی یک گواهیِ self-signedِ پین‌شده TLS حرف بزند |
| `IRNF_PROBE_MASK=1` | فرگمنترِ `finalmask` فورک را هم روی هاپ بگذارد |
| `IRNF_PROBE_DELAY` | تأخیرِ یک‌طرفه (میلی‌ثانیه) جلوی هاپ، تا زنجیره یک رفت‌وبرگشتِ واقعی داشته باشد |
| `IRNF_PROBE_NOBUF=1` | آن `bufferSize:0` ای که برای وایرگاردِ زنجیره‌ای گذاشته می‌شود را بردارد (برای آزمودنِ خودِ آن) |

- هسته‌ای که کانفیگ را اصلاً بار نکند **skip** می‌شود، نه fail — مثلاً `finalmask` مالِ فورک است و هسته‌ی رسمی آن را با `LengthMin can't be 0` رد می‌کند. این تست درباره‌ی **رفتار** است نه پشتیبانی از قابلیت.
- خروجی با یک خطِ جمع‌بندی تمام می‌شود («چند هسته از چند تا، وایرگارد شرکتی را از داخل زنجیره حمل کردند») و **کدِ خروجِ ۱ می‌دهد اگر حتی یکی شکست بخورد** — یعنی برای CI هم قابل‌اتکاست.

> این تست دقیقاً به این دلیل ساخته شد که نسخه‌ی ارزان‌ترِ قبلی‌اش — که فقط بسته‌های handshake را روی سوکتی می‌شمرد که هیچ‌وقت جواب نمی‌داد — نمی‌توانست «درخواست رفت» را از «تبادل کامل شد» تشخیص بدهد، و یک ریشه‌یابیِ اشتباه روی همان پایه منتشر شد. این یکی، به هر سؤال جواب می‌دهد.

#### `probe-dns-leak.js` — هیچ پرس‌وجویی نباید فرار کند

```bash
IRNF_XRAY_EXE=/path/to/xray node scripts/probe-dns-leak.js
```

کانفیگِ **واقعیِ** برنامه را روی loopback اجرا می‌کند و هر مسیر DNS را نه با حدس، بلکه با ساختِ آزمایش نسبت می‌دهد:

- resolver «داخلی» روی آدرسی از loopback می‌نشیند که فقط یک اتصالِ **مستقیم** می‌تواند به آن برسد؛
- resolver «خارجی» روی `198.18.0.1` است — آدرسی که روی هیچ کارتی از این دستگاه نیست، پس پرس‌وجو فقط از راهِ **خروجی تونل** می‌تواند برسد؛
- و `127.0.0.9:53` یک **سطل** است که هیچ‌جای کانفیگ نامش نیامده: هر بسته‌ای آن‌جا برسد یعنی **فرار کرده** به‌جای این‌که جواب داده شود.

با `IRNF_PROBE_BREAK=scope` یا `nonip` می‌شود عمداً نقشه را خراب کرد؛ هر دو باعث می‌شوند تست بیفتد — یعنی این پروبی است که **می‌تواند** شکست بخورد، و سبز بودنش معنا دارد.

چه چیزهایی را می‌سنجد: پوشش پورت ۵۳ روی TCP و UDP و هر دو خانواده‌ی آدرس، رفتار انواع پرس‌وجوی غیر-IP، دامنه‌ی اختیارِ resolver داخلی (از جمله وقتی resolver خارجی **مرده** است، که سخت‌ترین حالتش است)، و این‌که با خاموش بودن IPv6 هیچ سؤال AAAA بالا نرود.

---

## 🎨 ظاهر و رابط کاربری (UI)

- **رنگ‌بندی وضعیت:** هنگام اتصال، **تولبار و لوگو سبز** (با ✓) و هنگام قطع، **آبی با ✕ قرمز** می‌شوند.
- **نشان روی تسک‌بار ویندوز:** آیکون برنامه در نوار وظیفه نشان سبز (متصل) یا قرمز (قطع) می‌گیرد.
- **دکمه‌های پنجره:** کوچک‌کردن، **بزرگ‌نمایی/بازگردانی** (دکمه‌ی □)، و بستن به سینی سیستم (✕).
- **سینی سیستم (Tray):** نمایش، **سرورها به تفکیک ساب‌اسکریپشن** (یک کلیک = اتصال، سرورِ متصل علامت دارد)، قطع اتصال، خروج.
- **اعلان سیستمی:** هنگام قطع، بازیابی، انصراف از بازیابی و فعال‌شدن کیل‌سوییچ یک اعلان کوتاه و بی‌صدا؛ از تنظیمات خاموش می‌شود.
- **اجرا با سیستم‌عامل / اتصال خودکار:** از تنظیمات؛ روی ویندوز یک task زمان‌بندی‌شده با دسترسی ادمین ساخته می‌شود (برنامه ادمین است و login item معمولی برای برنامه‌های ادمین اجرا نمی‌شود). اگر سیستم‌عامل قبول نکرد، سوییچ برمی‌گردد و دلیلش گفته می‌شود.
- **زبان:** فارسی (RTL) و English، با یک کلیک.
- **تم:** تیره / روشن / مثل سیستم.

### سه ظاهر (Skins)

از **تنظیمات → ظاهر برنامه**. چیدمان و همه‌ی قابلیت‌ها در هر سه یکی است — ظاهر فقط توکن‌های رنگ و چند شکل (پهنای ریل کناری، گردیِ گوشه‌ها، شکل سوییچ و دکمه‌ی اتصال) را عوض می‌کند، پس عوض‌کردنش نمی‌تواند صفحه‌ای را بشکند. هر سه با هر دو تمِ تیره و روشن کار می‌کنند و پیش از اولین فریم اعمال می‌شوند (بدون فلشِ رنگ هنگام باز شدن).

| ظاهر | حال‌وهوا |
|---|---|
| **کنسول** (پیش‌فرض) | فیروزه‌ای، ریل برچسب‌دار، سوییچ‌های قرصی، دیالِ گرد، و یک نوار سیگنال بالای پنجره. |
| **کاکپیت** | پنل ابزار: نارنجی، ریل آیکونیِ ۶۴ پیکسلی، پنل‌های تخت با گوشه‌های تیز، اعداد مونواسپیس. |
| **کلاسیک** | همان ظاهری که برنامه تا v0.15.0 داشت: آبی، سایدبار پهنِ برچسب‌دار، دکمه‌ی اتصالِ گردِ گرادیانی. |

### نمای ساده / حرفه‌ای

سوییچی در نوار عنوان. «ساده» صفحه‌های زنجیره، استخر و لاگ و ویرایشگر قانون‌های سفارشی را کنار می‌گذارد. **هیچ چیزی که پنهان می‌شود برای وصل‌شدن لازم نیست** و یک کلیک برمی‌گردد. پیش‌فرضش حدس نیست: کسی که از قبل زنجیره، استخر یا قانون روتینگ دارد نمای حرفه‌ای می‌گیرد، پس یک به‌روزرسانی ابزار کسی را ناپدید نمی‌کند؛ نصب تازه تمیز شروع می‌کند.

### نمودار مسیر ترافیک

روی صفحه‌ی خانه نشان می‌دهد ترافیک **واقعاً** کجا می‌رود، و از روی کانفیگی که در انتخابگر انتخاب کرده‌اید ساخته می‌شود (نه از روی تنظیمات):

- یک سرور → دستگاه ← سرور ← اینترنت
- زنجیره → **همه‌ی هاپ‌ها** به ترتیب
- 🧭 روتینگ ویژه → هر قانون و مقصدش، با پیش‌فرض در انتها

هر هاپ **ترافیک خودش** را هم نشان می‌دهد (هسته شمارنده‌ها را به تفکیک outbound می‌دهد)، پس «چقدر از این کانفیگ رفته» در یک نگاه جواب دارد. نمودار فقط هنگام تغییر وضعیت اتصال یا ذخیره‌ی تنظیمات ساخته می‌شود — نه در تیک هر ثانیه‌ی آمار.

نمودار با **باریک شدن پنجره جمع می‌شود، نه این‌که بریده شود**: سرعتِ روی هر خط جای خودش را در چیدمان می‌گیرد (نه شناور روی خط) پس هیچ‌وقت روی کادر کناری نمی‌افتد، و فهرست قانون‌ها وقتی جا کم بیاید به سطر خودش می‌رود تا متن قانون با «…» بریده نشود. اندازه‌گیری‌شده روی کمینه‌ی عرض پنجره (۹۰۰ پیکسل) برای هر سه ظاهر و هر سه شکلِ نمودار.

---

## ⚙️ مرجع تنظیمات (Settings Reference)

| تنظیم | پیش‌فرض | توضیح |
|---|---|---|
| پورت SOCKS | `10808` | پورت inbound SOCKS5 |
| پورت HTTP | `10809` | پورت inbound HTTP |
| پورت آمار (API) | `10085` | آمار زنده‌ی ترافیک از این پورت روی `127.0.0.1` خوانده می‌شود |
| DNS خارجی | `https://1.1.1.1/dns-query, https://8.8.8.8/dns-query` | از داخل تونل |
| DNS داخلی | `178.22.122.100, 185.51.200.2` | مستقیم، فقط برای دامنه‌های ایرانی |
| مدیریت DNS | روشن | هسته هر DNS ای را که به آن برسد جواب می‌دهد |
| IPv6 | خاموش | فقط IPv4 |
| سطح لاگ | `warning` | none/error/warning/info/debug |
| پروکسی سیستمی | روشن | تنظیم خودکار پروکسی سیستم هنگام اتصال |
| حالت TUN | **روشن** | تونل کل سیستم. بدون بک‌اند نصب‌شده یا بدون دسترسی ادمین، اتصال با پیامی که می‌گوید چه چیزی را نصب کنید رد می‌شود — نه بازگشت بی‌صدا به حالت پروکسی |
| بک‌اندِ TUN | `sing-box` | `sing-box` (پیشنهادی) یا `tun2socks` (قدیمی) |
| گارد نشتی | `استاندارد` | خاموش / استاندارد (DNS همه‌ی کارت‌ها از تونل) / سخت‌گیرانه (هر چه از تونل نمی‌رود بسته می‌شود) — فقط حالت TUN |
| بستن UDP در حالت پروکسی | خاموش | جلوی WebRTC/STUN را می‌گیرد وقتی TUN روشن نیست (فقط ویندوز) |
| اجازه به LAN | خاموش | اشتراک پروکسی در شبکه محلی |
| اعلان سیستمی | روشن | قطع، بازیابی، انصراف از بازیابی، کیل‌سوییچ |
| اجرا با سیستم‌عامل | خاموش | task زمان‌بندی‌شده روی ویندوز؛ login item روی مک/لینوکس؛ با `--hidden` در سینی می‌ماند |
| اتصال خودکار | خاموش | یک ثانیه بعد از بالا آمدن پنجره به آخرین سرور وصل می‌شود |
| به‌روزرسانی خودکار فایل‌ها | `geo` | خاموش / فقط geo / geo و هسته‌ها — هفتگی، فقط وقتی وصل نیستید |
| کیل‌سوییچ | خاموش | بستن اینترنت هنگام قطع ناگهانی |
| حالت روتینگ | `گلوبال` | گلوبال / دور زدن ایران / دور زدن چین / مستقیم |
| روتینگ پیشرفته | خاموش | قانون‌های گرافیکیِ per-rule به‌جای حالت ساده |
| حالت روتینگ ساده هم اعمال شود | خاموش | حالت ساده را **زیرِ** قانون‌های پیشرفته هم اعمال می‌کند |
| به‌روز نگه‌داشتن مسیر پروسه‌ها | خاموش | مقصدهای تازه‌ی یک پروسه را حین اتصال هم اضافه می‌کند (ری‌لود کوتاه هسته) |
| بلاک تبلیغات | روشن | بلاک `geosite:category-ads-all` |
| Sniffing | روشن | تشخیص دامنه برای روتینگ دقیق‌تر |
| هسته‌ی پیش‌فرض | `xray` | هسته‌ای که کانفیگ‌های بدون انتخابِ اختصاصی روی آن اجرا می‌شوند |
| اتصال مجدد خودکار هنگام تغییر شبکه | روشن | جابه‌جایی Wi-Fi↔اترنت، lease تازه یا بیدارشدن از sleep، اتصال را خودکار بازسازی می‌کند |
| به‌روزرسانی خودکار ساب | روشن | با فاصله‌ی دلخواه (پیش‌فرض ۶۰ دقیقه) |
| زبان | `fa` | fa / en |
| تم | `تیره` | تیره / روشن / مثل سیستم |
| ظاهر برنامه | `کنسول` | کنسول / کاکپیت / کلاسیک — فقط رنگ و شکل، بدون تغییر چیدمان یا قابلیت |
| نمای ساده/حرفه‌ای | هوشمند | نصبی که از قبل زنجیره/استخر/قانون دارد «حرفه‌ای» شروع می‌کند، نصب تازه «ساده» |

> تنظیمات و کانفیگ‌ها فقط به‌صورت محلی در `userData/store.json` ذخیره می‌شوند. این فایل با `rename` اتمیک نوشته می‌شود، پس کرش یا قطع برقِ وسطِ ذخیره، فایل نیم‌بند به‌جا نمی‌گذارد؛ و فایل خرابِ احتمالی به‌جای نادیده‌گرفته‌شدن، با نام `.corrupt-<timestamp>` کنار می‌رود و برنامه می‌گوید چه شد.

### تنظیماتی که **با اتصال مجدد** اعمال می‌شوند

بیشترِ تنظیمات بالا در لحظه‌ی اتصال داخل `config.json` پخته می‌شوند (یا همان‌جا به سیستم اعمال می‌شوند: پروکسی سیستمی، کارت TUN، قانون فایروال LAN) و هسته برای هیچ‌کدام‌شان hot reload ندارد. پس عوض‌کردن‌شان وسطِ اتصال **هیچ کاری نمی‌کند** تا وقتی تونل دوباره ساخته شود.

برنامه این را بی‌صدا نمی‌گذارد بگذرد: از تنظیماتی که اتصالِ زنده **واقعاً با آن‌ها ساخته شده** یک عکس نگه می‌دارد، و اگر یکی از این‌ها فرق کند خودش می‌پرسد — و **اسمِ همان تنظیم‌ها را می‌گوید**، نه یک «تنظیماتی عوض شده»ی مبهم.

فهرست کامل ([`src/main/settingsMeta.js`](src/main/settingsMeta.js)): پورت‌های SOCKS/HTTP/API، اجازه به LAN، هر چهار تنظیم DNS، IPv6، سطح لاگ، Sniffing، حالت روتینگ، بلاک تبلیغات، قوانین سفارشی، روتینگ پیشرفته و تیکِ «حالت ساده هم اعمال شود» و قانون‌ها و مقصد پیش‌فرضش، به‌روز نگه‌داشتن مسیر پروسه‌ها، پروکسی سیستمی، حالت TUN، بک‌اندِ TUN، گارد نشتی، بستن UDP در حالت پروکسی، و هسته‌ی پیش‌فرض.

عمداً **بیرون** از این فهرست‌اند: **کیل‌سوییچ** (لحظه‌ی افتِ اتصال از روی تنظیمات خوانده می‌شود)، **به‌روزرسانی خودکار ساب و فاصله‌اش** (تایمر همان‌جا از نو کوک می‌شود) و **زبان/تم**.

---

## 🏗️ ساخت نسخه نصبی (Build)

**ویندوز:**
```bash
npm run dist          # NSIS installer + portable در پوشه‌ی dist/
```

**مک (Intel + Apple Silicon) — فقط روی خودِ macOS:**
```bash
npm run gen-icons     # یک‌بار، اگر assets/icon.icns نباشد
npm run dist:mac          # هر دو معماری
npm run dist:mac:x64      # فقط Intel
npm run dist:mac:arm64    # فقط M1/M2/M3/M4
```
خروجی: `IRNetFree-<version>-x64.dmg` و `IRNetFree-<version>-arm64.dmg` (و `.zip`).
> ⚠️ بیلد مک روی ویندوز ساخته نمی‌شود (به `hdiutil`/`codesign` نیاز است).

**اولین اجرا روی مک:** چون بدون گواهی Apple امضا شده، یک‌بار روی برنامه **راست‌کلیک → Open** بزنید یا `xattr -cr /Applications/IRNetFree.app` — جزئیات و چک‌لیست در [اولین اجرا روی مک](#-اولین-اجرا-روی-مک-macos--first-run).

**لینوکس:**
```bash
npm run dist:linux    # AppImage + deb
```

---

## 🐧 نسخه CLI / سرور بدون GUI (لینوکس، Headless)

روی یک سرور لینوکسی که **دسکتاپ/GUI ندارد** (VPS، سرور ابری، کانتینر…)، همین برنامه را به‌صورت **بدون واسط گرافیکی** اجرا کن. برنامه یک **وب‌سرور سبک** روی یک پورت محلی بالا می‌آورد که **دقیقاً همان رابط کاربری نسخه‌ی ویندوز/دسکتاپ** را نشان می‌دهد و **تمام امکانات** را دارد:

> پروکسی SOCKS/HTTP • **استخر پروکسی چندپورتی** • زنجیره‌ی پروکسی • ساب‌اسکریپشن (با به‌روزرسانی خودکار) • روتینگ ساده/پیشرفته/پروسه • حالت TUN • آمار زنده‌ی ترافیک • پینگ TCP و تأخیر واقعی • بررسی IP خروجی • مدیریت فایل‌های موردنیاز (xray/geo).

چرا سبک است؟ چون بک‌اند برنامه **هیچ وابستگی npm ندارد** و فقط با **Node.js core** کار می‌کند؛ پس نصب یعنی صرفاً «Node + سورس + باینری xray».

### 🧠 چطور کار می‌کند؟
به‌جای IPC داخلی Electron، یک پل ساده استفاده می‌شود:

```
مرورگر تو  ──HTTP/SSE──►  سرور Node روی سرور لینوکس  ──►  xray-core
(همان UI)      (پورت وب)        (src/server/*)            (پروکسی/تونل)
```

- `src/server/server.js` — وب‌سرور خالصِ Node: مسیر `POST /rpc` (فراخوانی توابع) + `GET /events` (رویدادهای زنده با SSE: لاگ/وضعیت/آمار) + سرو فایل‌های UI.
- `src/server/service.js` — همان منطق `main.js` دسکتاپ، بدون Electron (بازاستفاده‌ی کامل از ماژول‌های `src/main/*`).
- `src/server/web-api.js` — همان `window.api` را در مرورگر می‌سازد (با `fetch` و `EventSource`).

نتیجه: کدِ UI یکی است، رفتار یکی است؛ فقط لایه‌ی انتقال فرق دارد.

---

### 1) پیش‌نیازها
- یک سرور لینوکسی با دسترسی SSH (بدون نیاز به GUI).
- معماری CPU: `x86_64`/`amd64`، `arm64`/`aarch64` یا `armv7`.
- **نیازی به نصب دستی Node نیست** — اگر Node 18+ روی سیستم نباشد، اسکریپت نصب خودش یک نسخه‌ی portable دانلود می‌کند.
- برای حالت **پروکسی/استخر** نیازی به root **نیست**. فقط حالت **TUN** به root/sudo نیاز دارد.

---

### 2) نصب تک‌خطی (روی سرور)
روی خودِ سرور (از طریق SSH) این را بزن:
```bash
curl -fsSL https://raw.githubusercontent.com/sadrazkh/Irnetfree_xray-client/main/install.sh | bash
```
این اسکریپت به‌ترتیب:
1. اگر **Node.js 18+** نبود، یک Node **portable** مناسب معماری‌ات دانلود و کنار برنامه می‌گذارد (بدون دست‌زدن به سیستم).
2. سورس برنامه را از شاخه‌ی `main` می‌گیرد و در `~/.irnetfree/app` باز می‌کند.
3. **xray-core** را در `bin/` دانلود می‌کند.
4. یک لانچر آماده در `~/.irnetfree/run.sh` می‌سازد و دستور اجرا + راهنمای فوروارد پورت را چاپ می‌کند.

**فلگ‌های نصب** (با الگوی `| bash -s -- <flags>`):

| فلگ | کار |
|---|---|
| `--start` | بعد از نصب، بلافاصله سرور را اجرا کن (foreground) |
| `--service` | نصب و فعال‌سازی **سرویس systemd** (اجرای دائم + ری‌استارت خودکار؛ نیازمند sudo) |
| `--port <n>` | پورت وب UI (پیش‌فرض `6969`) |
| `--host <addr>` | آدرس bind (پیش‌فرض `127.0.0.1`؛ برای expose روی شبکه: `0.0.0.0`) |
| `--dir <path>` | مسیر نصب (پیش‌فرض `~/.irnetfree`) |

نمونه‌ها:
```bash
# نصب و اجرای فوری روی پورت 8080:
curl -fsSL https://raw.githubusercontent.com/sadrazkh/Irnetfree_xray-client/main/install.sh | bash -s -- --start --port 8080

# نصب دائمی به‌عنوان سرویس systemd (بالا می‌ماند و بعد ریبوت خودکار اجرا می‌شود):
curl -fsSL https://raw.githubusercontent.com/sadrazkh/Irnetfree_xray-client/main/install.sh | bash -s -- --service
```

---

### 3) اجرا
اگر بدون `--start`/`--service` نصب کردی، سرور را دستی اجرا کن:
```bash
~/.irnetfree/run.sh                    # روی 127.0.0.1:6969
~/.irnetfree/run.sh --port 8080        # پورت دلخواه
~/.irnetfree/run.sh --host 0.0.0.0     # expose روی شبکه (توکن اجباری می‌شود)
```
اگر پوشه‌ی `~/.local/bin` در PATH‌ات باشد، یک میان‌بر هم ساخته می‌شود:
```bash
irnetfree --port 6969
```

هنگام اجرا، خروجی چیزی شبیه این است:
```
  IRNetFree server (headless) — v0.9
  Data dir : /home/user/.local/share/irnetfree
  Listening: http://127.0.0.1:6969/
  ...ssh -N -L 6969:127.0.0.1:6969 user@SERVER...
```

---

### 4) دسترسی از روی لپ‌تاپ (فوروارد پورت) — گام‌به‌گام
سرور به‌طور **پیش‌فرض فقط روی `127.0.0.1`** گوش می‌دهد (امن‌ترین حالت؛ از بیرون قابل دسترسی نیست). برای دیدن پنل، پورت را با SSH به لپ‌تاپت **tunnel** کن:

**۱.** روی سرور، سرور را اجرا کن (یا سرویس systemd روشن باشد).
**۲.** روی **لپ‌تاپ خودت** یک ترمینال باز کن و بزن:
```bash
ssh -N -L 6969:127.0.0.1:6969 user@YOUR_SERVER_IP
```
（`-N` یعنی فقط تونل، بدون شل. این ترمینال را باز نگه دار.）
**۳.** حالا در **مرورگر لپ‌تاپت** برو به:
```
http://127.0.0.1:6969/
```
همین! پنل کامل بالا می‌آید — انگار برنامه‌ی دسکتاپ را باز کرده‌ای.

> 💡 روی ویندوز، هم `ssh` داخلیِ ویندوز کار می‌کند، هم PuTTY (در PuTTY: Connection → SSH → Tunnels → Source `6969`, Destination `127.0.0.1:6969`).

---

### 5) استفاده از پنل (همان کارهای دسکتاپ)
پس از باز شدن پنل روی مرورگر:

1. **افزودن کانفیگ:** تب **سرورها** → «+ افزودن» و لینک `vless://` / `vmess://` / `trojan://` / `ss://` / `socks://` را بچسبان (یا «+ SOCKS/HTTP» / «+ وایرگارد»). لینک ساب‌اسکریپشن (`https://…`) هم در تب **ساب‌ها** اضافه می‌شود.
2. **اتصال ساده:** تب **اتصال** → سرور را انتخاب و دکمه‌ی روشن/خاموش را بزن. با «بررسی IP» مطمئن شو.
3. **استخر پروکسی (چند خروجی هم‌زمان):** تب **استخر پروکسی** → «+ پروکسی جدید» → برای هر ردیف یک خروجی و یک **پورت SOCKS** (مثلاً `60001`, `60002`, …) بگذار → «اتصال استخر». حالا چند پروکسی هم‌زمان روی پورت‌های جدا داری.
4. **استفاده از پورت‌های پروکسی روی سرور:** پورت‌های SOCKS/HTTP (پیش‌فرض `10808`/`10809` و پورت‌های استخر مثل `60001`) روی **`127.0.0.1` سرور** باز می‌شوند. دو راه استفاده:
   - **از روی خود سرور:** برنامه‌ها را به `127.0.0.1:10808` (یا پورت استخر) وصل کن؛ مثلاً:
     ```bash
     curl --socks5-hostname 127.0.0.1:10808 https://ifconfig.me
     export ALL_PROXY=socks5://127.0.0.1:60001   # اپ‌های دیگر
     ```
   - **از روی لپ‌تاپ:** همان پورت پروکسی را هم SSH-forward کن و در مرورگر/برنامه‌ات به‌عنوان پروکسی SOCKS بگذار:
     ```bash
     ssh -N -L 10808:127.0.0.1:10808 -L 60001:127.0.0.1:60001 user@YOUR_SERVER
     # سپس پروکسی SOCKS5 برنامه = 127.0.0.1:10808  (یا 60001)
     ```
   - **در دسترس گذاشتن مستقیم روی شبکه:** در تنظیمات، «اجازه به شبکه محلی (LAN)» را روشن کن تا پورت‌های پروکسی روی `0.0.0.0` سرور باز شوند (فقط اگر فایروال/شبکه‌ات امن است).

> نکته: در نسخه‌ی سرور، «پروکسی سیستمی» پیش‌فرض **خاموش** است (سرور رابط گرافیکی ندارد که پروکسی سیستمی برایش معنی داشته باشد). استفاده‌ی اصلی، همان پورت‌های SOCKS/HTTP و استخر است.

---

### 6) سرویس دائمی با systemd
اگر با `--service` نصب کردی، سرویس `irnetfree` ساخته و فعال شده. مدیریتش:
```bash
systemctl status irnetfree      # وضعیت
systemctl restart irnetfree     # ری‌استارت
systemctl stop irnetfree        # توقف
journalctl -u irnetfree -f      # دیدن لاگ زنده
```
اگر بعداً خواستی پورت/هاست سرویس را عوض کنی، فایل `/etc/systemd/system/irnetfree.service` را ویرایش کن (خط `ExecStart`)، بعد:
```bash
sudo systemctl daemon-reload && sudo systemctl restart irnetfree
```

### اجرای دستی در پس‌زمینه (بدون systemd)
```bash
nohup ~/.irnetfree/run.sh --port 6969 >~/.irnetfree/server.log 2>&1 &
# توقف:  pkill -f src/server/server.js
```

---

### 7) اجرای مستقیم از سورس (بدون اسکریپت نصب)
اگر ریپو را کلون کرده‌ای:
```bash
npm run get-xray                              # یک‌بار، برای دانلود هسته xray
npm run server -- --port 6969 --host 127.0.0.1
# یا مستقیم:
node src/server/server.js --port 6969
```

---

### 8) امنیت 🔒
- **bind پیش‌فرض `127.0.0.1`** است؛ یعنی پنل فقط از طریق **SSH-forward** در دسترس است و از اینترنت باز نیست. این امن‌ترین و توصیه‌شده‌ترین حالت است.
- اگر با `--host 0.0.0.0` روی شبکه/اینترنت expose کنی، **توکن اجباری** می‌شود:
  - اگر `--token` ندهی، یک توکن تصادفی ساخته و در URL چاپ می‌شود (`http://IP:PORT/?token=...`).
  - می‌توانی خودت بدهی: `--token 'یک‌رشته‌ی‌قوی'`.
  - **هر** درخواست بدون توکن درست (`?token=` یا هدر `x-irnetfree-token`) رد می‌شود — حتی خودِ صفحه.
- چون این پنل به هسته‌ی VPN دسترسی دارد، **هرگز** بدون توکن (و ترجیحاً بدون HTTPS/reverse-proxy) مستقیم روی اینترنت بازش نکن. برای دسترسی از راه دور، **SSH-forward** بهترین گزینه است.

**همه‌ی گزینه‌های خط فرمان:**

| گزینه | پیش‌فرض | توضیح |
|---|---|---|
| `--port <n>` | `6969` | پورت وب UI |
| `--host <addr>` | `127.0.0.1` | آدرس bind (`0.0.0.0` برای شبکه) |
| `--token <secret>` | — | توکن دسترسی (روی bind غیرلوکال اجباری) |
| `--data-dir <path>` | `~/.local/share/irnetfree` | محل ذخیره‌ی کانفیگ‌ها/تنظیمات |
| `--no-auth` | — | غیرفعال‌کردن توکن حتی روی bind عمومی (خطرناک) |

---

### 9) به‌روزرسانی و حذف
**به‌روزرسانی** (دوباره اجرای همان نصب‌کننده؛ داده‌ها در `data-dir` جدا هستند و پاک نمی‌شوند):
```bash
curl -fsSL https://raw.githubusercontent.com/sadrazkh/Irnetfree_xray-client/main/install.sh | bash
sudo systemctl restart irnetfree   # اگر سرویس داری
```
**حذف کامل:**
```bash
sudo systemctl disable --now irnetfree 2>/dev/null; sudo rm -f /etc/systemd/system/irnetfree.service
rm -rf ~/.irnetfree                 # برنامه + Node portable
rm -rf ~/.local/share/irnetfree     # داده‌ها/تنظیمات (اگر می‌خواهی پاک شوند)
```

---

### 10) عیب‌یابی سرور
| مشکل | راه‌حل |
|---|---|
| **مرورگر باز نمی‌شود / Connection refused** | مطمئن شو سرور روی سرور در حال اجراست و SSH-forward برقرار است؛ پورت لوکال و ریموت را چک کن. |
| **`Port ... already in use`** | پورت دیگری بده: `--port 6970`. |
| **«هسته Xray پیدا نشد»** | `cd ~/.irnetfree/app && node scripts/download-xray.js` (یا از پنل: تنظیمات → فایل‌های موردنیاز → دانلود). |
| **روی bind عمومی صفحه `unauthorized` می‌دهد** | با توکن باز کن: `http://IP:PORT/?token=...` (توکن در خروجی سرور چاپ شده). |
| **حالت TUN کار نمی‌کند** | سرور را با `sudo` اجرا کن (حالت TUN **root** می‌خواهد) و `sing-box` را دانلود کن (یا `tun2socks` را)؛ برای اکثر کاربردها **پروکسی/استخر** کافی و بی‌دردسرتر است. |
| **آمار/رویدادها آپدیت نمی‌شود** | مرورگر باید از `EventSource`(SSE) پشتیبانی کند (همه‌ی مرورگرهای مدرن می‌کنند)؛ اگر پشت reverse-proxy هستی، بافرینگ SSE را خاموش کن. |

---

## 🤖 نسخه اندروید (Android)

نسخه‌ی اندروید یک **اپ نیتیو جدا (Kotlin + Jetpack Compose)** در پوشه‌ی [`android/`](android) است (کلاینت دسکتاپ Electron روی اندروید اجرا نمی‌شود). این اپ همان منطق کانفیگ‌سازیِ دسکتاپ را دارد (تک‌کانفیگ، **زنجیره**، **استخر پروکسی**، **SOCKS/HTTP**، روتینگ) و ترافیک را با **`VpnService` + `hev-socks5-tunnel`** به هسته‌ی **xray-core** (از طریق `libv2ray`) تونل می‌کند.

**ساخت محلی:**
```bash
cd android
bash scripts/fetch-libs.sh   # دانلود libv2ray.aar (لازم برای کامپایل) + در صورت تنظیم، .soی tun2socks
gradle wrapper               # یک‌بار، اگر ./gradlew نداری (Wrapper در گیت نگه‌داری نمی‌شود)
./gradlew assembleRelease    # خروجی: app/build/outputs/apk/release/*.apk
# یا برای دیباگ:
./gradlew assembleDebug
```
> در Android Studio کافی است پوشه‌ی `android/` را باز کنی (Sync خودش وابستگی‌ها را می‌گیرد؛ فقط `fetch-libs.sh` را یک‌بار اجرا کن تا `libv2ray.aar` سر جایش باشد).

- **حداقل نسخه:** Android 8.0 (API 26). **هدف:** API 34.
- دو مؤلفه‌ی نیتیو (`libv2ray.aar` و `libhev-socks5-tunnel.so`) در گیت نگه‌داری **نمی‌شوند**؛ اسکریپت `fetch-libs.sh` (و CI) آن‌ها را از ریلیزهای بالادست دانلود می‌کند.
- امضای ریلیز: اگر رازهای keystore در گیت‌هاب تنظیم شده باشند، APKِ **امضاشده** ساخته می‌شود؛ در غیر این صورت APKِ **دیباگ** برای تست تولید می‌شود (به بخش بعد).

> ⚠️ رفتار واقعیِ تونل روی **دستگاه/شبیه‌ساز** باید تست شود؛ نسخه‌ی دقیق `libv2ray` باید با امضای API در سرویس VPN بخواند (در صورت تغییر API بالادست، فقط `XrayVpnService.kt` نیاز به تطبیق دارد).

---

## 🚀 انتشار خودکار (GitHub Actions Release)

با **push کردن یک تگ** به شکل `v*` (مثلاً `v0.9.0`)، ورک‌فلو [`.github/workflows/release.yml`](.github/workflows/release.yml) هم **دسکتاپ** و هم **APK اندروید** را می‌سازد و همه را به همان **GitHub Release** پیوست می‌کند:

```bash
git tag v0.9.0
git push origin v0.9.0
```

خروجی‌های ریلیز:
- ویندوز: `IRNetFree-Setup-<v>.exe` و `IRNetFree-Portable-<v>.exe`
- لینوکس: `IRNetFree-<v>-x86_64.AppImage` و `.deb`
- **اندروید: `IRNetFree-<v>.apk`**
- (مک روی یک ranner جدا اگر فعال باشد)

**امضای اندروید (اختیاری ولی توصیه‌شده):** برای APKِ امضاشده‌ی release، این رازها را در گیت‌هاب (`Settings → Secrets and variables → Actions`) بگذارید:

| Secret | توضیح |
|---|---|
| `ANDROID_KEYSTORE_BASE64` | فایل keystore به‌صورت base64 (`base64 -w0 my.keystore`) |
| `ANDROID_KEYSTORE_PASSWORD` | رمز keystore |
| `ANDROID_KEY_ALIAS` | نام alias کلید |
| `ANDROID_KEY_PASSWORD` | رمز کلید |

اگر این رازها نباشند، CI به‌جای شکست، **APKِ دیباگ** را می‌سازد و پیوست می‌کند تا همیشه یک فایلِ قابل‌نصب داشته باشید.

---

## 🏛️ معماری (Architecture)

برنامه سه لایه دارد و **دو میزبان**:

```
دسکتاپ:   renderer/  ──IPC──►  preload/preload.js  ──►  main/main.js
سرور:     renderer/  ──HTTP+SSE──►  server/server.js  ──►  server/service.js

                        هر دو به یک جا می‌رسند
                                 ▼
                            src/main/*.js
        (پارسر، سازنده‌ی کانفیگ، DNS، روتینگ، انتخاب هسته، TUN، گارد، آمار، استور)
                                 ▼
                   xray-core  /  xray-pattn  /  sing-box
```

- **فرآیند اصلی (`src/main/`)** همه‌ی کارهایی را می‌کند که به سیستم‌عامل و به هسته‌ها دست می‌زنند: خواندن و ساختن کانفیگ، بالا/پایین‌کردن پروسه‌ها، کارت TUN، فایروال، پروکسی سیستمی، دانلودها.
- **رندرر (`src/renderer/`)** هیچ دسترسی مستقیمی به Node یا سیستم ندارد؛ فقط از طریق `preload/preload.js` با فرآیند اصلی حرف می‌زند (`contextIsolation` روشن، `nodeIntegration` خاموش).
- **نسخه‌ی سرور (`src/server/`) یک آینه است، نه یک شاخه‌ی دیگر.** `service.js` **همان ماژول‌های `src/main/*.js`** را require می‌کند و فقط Electron را از وسط برمی‌دارد؛ `server.js` به‌جای IPC یک `POST /rpc` و یک `GET /events` (SSE) می‌گذارد و `web-api.js` همان `window.api` را در مرورگر می‌سازد. نتیجه این است که **یک منطق و یک رابط کاربری** وجود دارد و فقط لایه‌ی انتقال فرق می‌کند — نه دو پیاده‌سازی که با هم از هم دور بیفتند.

به همین دلیل، هر قابلیتی که در `src/main/` اضافه شود باید در **هر دو** نگه‌دارنده وصل شود (`main.js` و `src/server/service.js`).

**`tests/` شکلِ خروجی سازنده‌ی کانفیگ را قفل می‌کند.** بیشترِ ماژول‌های `src/main/` عمداً *pure* نوشته شده‌اند — دستورهای سیستمی، ساعت و خواننده‌ی کارت شبکه از بیرون تزریق می‌شوند — تا بشود بدون بالا آوردن هیچ تونل و بدون هیچ کارت شبکه‌ی واقعی، دقیقاً همان چیزی را سنجید که قرار است اجرا شود. مثلاً متنِ اسکریپت‌هایی که با دسترسی Administrator روی دستگاه شما اجرا می‌شوند، تنها بازبینی‌ای که پیش از اجرا می‌بینند همین تست‌هاست. تست‌های `configBuilder` هم «golden» اند: اگر تنظیمی که نباید روی خروجی اثر بگذارد اثر بگذارد، تست می‌شکند.

```bash
npm test          # همه‌ی تست‌های واحد، بدون نیاز به هیچ هسته‌ای
```

---

## 📁 ساختار پروژه (Project Structure)

```
src/
  main/
    main.js          # فرآیند اصلی Electron، IPC، چرخه‌ی عمر، LAN/کیل‌سوییچ/overlay
    parser.js        # تبدیل لینک‌ها (vless/vmess/trojan/ss/socks/wireguard) و فایل .conf به outbound
    configBuilder.js # ساخت config.json کامل (inbounds + routing + حالت pool چندپورتی)
    dnsBuilder.js    # نقشه‌ی resolver: DoH بیرونی، resolver داخلی، رهگیری پورت ۵۳، DNS مقصدها
    certPin.js       # پین‌کردن گواهی در اولین استفاده (جایگزین allowInsecure)
    geoCheck.js      # آیا کدهای geoip/geosite واقعاً در فایل‌های نصب‌شده هستند؟
    xrayManager.js   # مدیریت پروسه‌ی هسته (start/stop/test/version) + برگشت خودکار به PattN
    engines.js       # رجیستری هسته‌ها: xray (رسمی)، xray-pattn (پترنیها)، sing-box
    engineChoice.js  # کدام هسته یک plan را اجرا می‌کند (انتخاب کانفیگ / هسته‌ی پیش‌فرض)
    settingsMeta.js  # کدام تنظیمات داخل اتصال پخته شده‌اند و اتصال مجدد می‌خواهند
    settingsMigrate.js # ارتقای تنظیمات قدیمی (فهرست DNS تکی → خارجی/داخلی)
    assets.js        # وضعیت فایل‌های موردنیاز (مشترک دسکتاپ و headless)
    subscription.js  # ساب‌اسکریپشن + خواندن حجم/زمان از هدر Subscription-Userinfo
    tunSingbox.js    # حالت TUN روی sing-box (بک‌اند پیش‌فرض؛ auto_route، v4+v6)
    tunManager.js    # حالت TUN روی tun2socks (بک‌اند قدیمی)
    tunPlatform.js   # تفاوت‌های سیستم‌عاملیِ تونل (اجرای دستور با دسترسی بالا، کارت‌ها، …)
    leakGuard.js     # گارد نشتی: DNS کارت‌ها، فایروال سخت‌گیرانه، بازیابی بعد از کرش
    singboxBuilder.js # ترجمه‌ی یک کانفیگ تکی به فرمت sing-box
    netWatcher.js    # تشخیص تغییر شبکه‌ی دستگاه زیرِ یک تونلِ زنده
    procRouter.js    # کشف پروسه‌ها و IPهایشان برای روتینگ پراسس
    stats.js         # آمار زنده‌ی ترافیک + تشخیص تونلی که جواب نمی‌دهد
    sysproxy.js      # پروکسی سیستمی (Win/macOS/Linux)
    netutils.js      # پینگ TCP، تأخیر دانلود/آپلود، بررسی IP/geo
    downloader.js    # دانلود/به‌روزرسانی هسته‌ها و فایل‌های جانبی
    store.js         # ذخیره‌ساز JSON (نوشتن اتمیک، نگه‌داشتن فایل خراب)
  preload/preload.js # پل امن IPC
  renderer/          # رابط کاربری (index.html / app.js / i18n.js / theme-boot.js)
                     # استایل به تفکیک صفحه: styles + home + lists + routing + settings + skins
  server/            # نسخه‌ی headless (لینوکس/CLI): همان بک‌اند روی وب
    service.js       # هسته‌ی بدون‌Electron (بازاستفاده از همه‌ی ماژول‌های main/)
    server.js        # وب‌سرور HTTP + RPC + SSE (فقط Node core)
    guard.js         # توکن، bind و محافظت در برابر DNS rebinding
    web-api.js       # پلِ window.api در مرورگر (fetch/EventSource به‌جای IPC)
install.sh           # نصب‌کننده‌ی تک‌خطی لینوکس (Node + سورس + xray + لانچر/systemd)
scripts/
  download-xray.js   # دانلودر هسته
  validate-configs.js # هر شکلِ کانفیگ را به هسته می‌دهد (npm run validate)
  probe-wg-chain.js  # سناریوی وایرگارد شرکتی، سرتاسری (npm run probe:wg)
  probe-dns-leak.js  # هر مسیر DNS را دنبال و نسبت می‌دهد؛ سطلی دارد که هیچ‌جای کانفیگ نامش نیست
  mac-selfcheck.sh   # خودآزمای macOS (فقط‌خواندنی؛ خروجی‌اش را در ایشو بچسبانید)
  gen-icons.js       # ساخت آیکون‌های برنامه از assets/icon.png
tests/               # تست‌های واحد؛ شکلِ خروجی سازنده‌ی کانفیگ را قفل می‌کنند
android/             # اپ نیتیو اندروید (Kotlin + Compose)
  app/src/main/java/com/irnetfree/vpn/
    core/            # LinkParser, ConfigBuilder (پورت‌شده از JS، شامل pool/socks), Models, Store
    vpn/             # XrayVpnService (VpnService + libv2ray + hev-socks5-tunnel)
    ui/              # صفحه‌های Compose (سرورها / استخر / اتصال)
  scripts/fetch-libs.sh   # دانلود libv2ray.aar + libhev-socks5-tunnel .so
.github/workflows/
  release.yml        # ساخت دسکتاپ + APK اندروید روی تگ و پیوست به Release
  test.yml           # تست‌های واحد روی لینوکس/ویندوز/مک + کامپایل اپ اندروید، در هر push و PR
```

---

## 🔒 امنیت (Security)

- `contextIsolation` فعال و `nodeIntegration` غیرفعال؛ رندرر فقط از طریق preload با main حرف می‌زند.
- هنگام خروج/قطع، پروکسی سیستمی، روت‌های TUN، قانون LAN، قانون‌های گارد نشتی و کیل‌سوییچ **خودکار پاک‌سازی** می‌شوند — و آن‌چه با کرش یا قطع برق پاک نشده باشد، در اجرای بعدی از روی `userData/tun-state.json` برگردانده می‌شود.
- **`allowInsecure` هرگز داخل کانفیگ نوشته نمی‌شود.** به‌جایش گواهیِ سرور در اولین استفاده پین می‌شود، و هسته از آن به بعد فقط همان یک گواهی را می‌پذیرد → [پین‌کردن گواهی](#-پینکردن-گواهی-certificate-pinning).
- در حالت TUN، هر dial ای که خودِ هسته می‌زند (خروجی `direct`، هاپ اولِ زنجیره، endpoint وایرگارد) به کارت شبکه‌ی **فیزیکی** بسته می‌شود، وگرنه از تونل رد می‌شد و در حلقه می‌افتاد — به همین دلیل «دور زدن ایران» زیر TUN هم واقعاً مستقیم می‌رود.
- کلیدها و کانفیگ‌ها فقط **محلی** ذخیره می‌شوند؛ چیزی به سروری ارسال نمی‌شود (به‌جز خود ساب‌ها که شما اضافه می‌کنید).
- در نسخه‌ی سرور، bind پیش‌فرض `127.0.0.1` است و روی هر bind غیرلوکال **توکن اجباری** می‌شود (بخش [نسخه CLI / سرور بدون GUI](#-نسخه-cli--سرور-بدون-gui-لینوکس-headless)).

---

## 🛠️ عیب‌یابی (Troubleshooting)

### مشکل‌های عمومی

| مشکل | راه‌حل |
|---|---|
| **«هسته Xray پیدا نشد»** | تنظیمات → فایل‌های موردنیاز → دانلود (یا `npm run get-xray`). |
| **وصل شد ولی اینترنت ندارید** | حالت روتینگ را روی «گلوبال» بگذارید؛ پورت‌ها و سرور را چک کنید؛ «بررسی IP» را بزنید. |
| **تنظیمی را عوض کردید و هیچ اتفاقی نیفتاد** | بیشترِ تنظیمات در لحظه‌ی اتصال داخل کانفیگ پخته می‌شوند؛ **یک‌بار دوباره وصل شوید**. برنامه معمولاً خودش می‌پرسد و اسم تنظیم را می‌گوید → [تنظیماتی که با اتصال مجدد اعمال می‌شوند](#تنظیماتی-که-با-اتصال-مجدد-اعمال-میشوند). |
| **حالت TUN کار نمی‌کند (ویندوز)** | برنامه را با **ادمین** اجرا کنید — پیام دقیقش این است: `TUN mode needs Administrator rights`. مطمئن شوید `sing-box` (یا `tun2socks`) و `wintun.dll` نصب‌اند — لاگ می‌گوید کدام بک‌اند اجرا شده. اگر کارت بالا نیامد: `TUN adapter did not become ready — check admin rights and wintun.dll`. |
| **TUN روی مک کار نمی‌کند** | هنگام اتصال رمز عبور را بدهید (**تا دو پنجره**: یکی برای تونل، یکی برای گارد نشتی)؛ `sing-box` (یا tun2socks) را از «فایل‌های موردنیاز» دانلود کنید؛ پیش از خروج، اول **قطع** کنید. اگر باز هم نشد: `npm run selfcheck:mac` و خروجی کامل را گزارش کنید ([راهنما](#-اولین-اجرا-روی-مک-macos--first-run)). |
| **DNS بعد از کرش عوض مانده** | برنامه را دوباره باز کنید؛ گارد نشتی مقدارهای اصلی را از `userData/tun-state.json` برمی‌گرداند و در لاگ می‌نویسد. روی مک یک‌بار رمز می‌پرسد. |
| **با گارد «سخت‌گیرانه»، سایت‌های ایرانی باز نمی‌شوند** | طبیعی است: این سطح هر چیزی را که از تونل نرود می‌بندد، و «دور زدن ایران» دقیقاً ترافیک را از کارت فیزیکی بیرون می‌فرستد. یا گارد را «استاندارد» کنید یا روتینگ را «گلوبال». برنامه خودش هم این تناقض را هشدار می‌دهد. |
| **گارد «سخت‌گیرانه» است ولی IPv6 هنوز نشت می‌کند** | احتمالاً بک‌اند tun2socks اجرا شده (وقتی sing-box نصب نباشد). روی آن بک‌اند نه `strict_route` هست و نه مسیر IPv6، و قانون‌های فایروال هم فقط IPv4اند. **sing-box** را نصب کنید. |
| **روتینگ geoip/geosite بی‌اثر است** | فایل‌های `geoip.dat` و `geosite.dat` را دانلود کنید. بدونشان، قانون‌های `geoip:`/`geosite:` کنار گذاشته می‌شوند (وگرنه هسته اصلاً بالا نمی‌آمد) و برنامه هشدار می‌دهد. |
| **LAN کار نمی‌کند** | برنامه را با ادمین اجرا کنید (فایروال)؛ روی دستگاه دیگر IP:پورت درست را بزنید؛ یک‌بار دوباره وصل شوید. |
| **کیل‌سوییچ فعال نمی‌شود** | با **ادمین** اجرا کنید (با فایروال کار می‌کند). |
| **اینترنت بعد از کرش بسته ماند** | برنامه را دوباره باز کنید؛ هنگام شروع، قانون کیل‌سوییچ پاک می‌شود. |
| **ساب دریافت نمی‌شود** | آدرس باید با `http(s)://` شروع شود و در دسترس باشد. |
| **پروکسی سیستمی پاک نشد** | برنامه را دوباره باز کنید؛ هنگام شروع پاک‌سازی می‌شود. |
| **بعد از تغییر Wi-Fi / بیدارشدن از sleep، «متصل» است ولی چیزی رد نمی‌شود** | باید خودکار بازسازی شود؛ اگر تنظیم «اتصال مجدد خودکار هنگام تغییر شبکه» را خاموش کرده‌اید، دکمه‌ی **«اتصال مجدد»** روی صفحه‌ی خانه را بزنید (بهتر از قطع و وصلِ دستی: گارد نشتی را در فاصله‌اش رها نمی‌کند). |
| **در حین اتصال مجدد هیچ سایتی باز نمی‌شود** | این عمدی است: گارد نشتی نگه داشته می‌شود تا هیچ پرس‌وجویی به ISP نرسد، پس تا برگشتنِ تونل نام‌ها resolve نمی‌شوند. چند ثانیه طول می‌کشد. |
| **بنر «اینترنتم را برگردان» آمده** | یعنی تلاش‌های اتصال مجدد به نتیجه نرسیده و گارد هنوز نگه داشته شده — چیزی نشت نمی‌کند ولی چیزی هم باز نمی‌شود. یا «تلاش دوباره» را بزنید، یا با آن دکمه DNS کارت‌ها را برگردانید (از آن به بعد دوباره از ISP می‌پرسید). |
| **Cmd+Q روی مک فقط پنجره را می‌بست** | در v1.2.0 درست شد: حالا `Cmd+Q`، Dock → Quit و logout واقعاً خارج می‌شوند و teardown را هم اجرا می‌کنند. کلیک روی آیکون Dock پنجره را برمی‌گرداند. |
| **روی مک «پروکسی سیستم فعال شد» می‌گفت ولی چیزی عوض نشده بود** | در v1.2.0 درست شد: `networksetup` برای نوشتن ادمین می‌خواهد و خطایش بلعیده می‌شد. حالا اگر هیچ سرویسی تنظیم را نپذیرد، شکست گزارش می‌شود. |

### وقتی هسته کانفیگ را رد می‌کند

| پیام هسته | یعنی چه، و چه کار کنید |
|---|---|
| `code not found in geosite.dat: IR` (یا هر کد دیگر) | **فایل‌های geo سالم‌اند؛ کدِ قانون اشتباه است.** یک کد ناموجود باعث می‌شود **کل** کانفیگ رد شود و پیام مثل «فایل geo نداری» به نظر می‌رسد. مشهورترین نمونه: `geosite:ir` **وجود ندارد** — فهرست دامنه‌های ایران **`geosite:category-ir`** است (اما `geoip:ir` وجود دارد). برنامه هنگام ذخیره‌ی قانون‌ها همین را چک می‌کند و کد خراب را اسم می‌برد. |
| `without TLS ... prohibited` | کانفیگ VLESS/Trojan بدون TLS به یک آدرس عمومی است و هسته‌ی رسمی آن را نمی‌پذیرد. **Xray-PattN** را از تنظیمات → فایل‌های موردنیاز نصب کنید؛ برنامه خودش کانفیگ را روی آن اجرا می‌کند. |
| `LengthMin can't be 0` روی کانفیگی که `finalMask` دارد | `finalMask` مالِ فورک پترنیهاست و هسته‌ی رسمی نمی‌شناسدش. در فرم ویرایشِ همان کانفیگ، هسته را روی **Xray-PattN** بگذارید. |
| `removed and migrated to pinnedPeerCertSha256` | کانفیگ هنوز `allowInsecure` دارد. برنامه‌ی به‌روز این را خودش برمی‌دارد و به‌جایش گواهی را پین می‌کند → [پین‌کردن گواهی](#-پینکردن-گواهی-certificate-pinning). |
| `Unable to update bind: lookup <host>: no such host` | endpoint وایرگارد یک **نام** است و هسته‌ی PattN آن را با DNS خودش resolve نمی‌کند. برنامه‌ی به‌روز نام را از پیش به آدرس تبدیل می‌کند؛ اگر خودِ نام از این دستگاه resolve نشود، در فرم به‌جایش IP بگذارید. |
| `first path segment in URL cannot contain colon` | یک ورودیِ DNS به شکل `host:port` نوشته شده که هسته به‌صورت رشته قبول نمی‌کند. برنامه‌ی به‌روز خودش تبدیلش می‌کند؛ اگر `store.json` را دستی ویرایش کرده‌اید، از همان فرم تنظیمات دوباره ذخیره کنید. |
| **پورت اشغال است** | پورت SOCKS/HTTP/آمار یا یکی از پورت‌های استخر با چیز دیگری تداخل دارد. پورت را عوض کنید — دقت کنید پورت آمار (`10085`) هم رزرو است و یک ردیف استخر نمی‌تواند بگیردش. |

### وایرگارد شرکتی

| مشکل | راه‌حل |
|---|---|
| **سایت‌های شرکت باز نمی‌شوند، ولی بقیه‌ی اینترنت سالم است** | اول لاگ را ببینید. اگر پیام «ترافیک به این تونل فرستاده می‌شود ولی هیچ پاسخی برنمی‌گردد» آمده، **handshake کامل نمی‌شود**: endpoint و پورت را چک کنید، و اگر از داخل زنجیره می‌رود، این‌که هاپِ قبلی UDP را عبور می‌دهد یا نه. اگر چنین پیامی **نیامده**، تونل زنده است و مشکل جای دیگری است (روتینگ یا DNS). |
| **endpoint وایرگارد یک نام است و روی PattN بالا نمی‌آید** | همان `Unable to update bind` بالا؛ نسخه‌ی به‌روز خودش حلش می‌کند. |
| **نام‌های داخلی (`gitlab.corp…`) resolve نمی‌شوند** | در فرم همان سرور، فیلد **DNS** را با resolver داخلی پر کنید و **دامنه‌های جست‌وجو** را با کاما بعدش بگذارید (`192.168.60.1, tes.systems`). بدون دامنه‌ی جست‌وجو، اول از resolver عمومی پرسیده می‌شود و فقط `NXDOMAIN` اش به DNS شرکت می‌افتد. |
| **با روشن‌کردن روتینگ پیشرفته، کلِ اینترنت قطع شد** | احتمالاً یک وایرگاردِ **split** را «مقصد پیش‌فرض» کرده‌اید؛ هر مقصدی جز رنج‌های خودش داخل تونل دور ریخته می‌شود. برای ترافیک عادی یک سرور دیگر را پیش‌فرض کنید و وایرگارد را فقط مقصدِ قانونِ رنج شرکت بگذارید. |
| **وایرگارد از داخل زنجیره داده رد نمی‌کند** | نسخه‌ی به‌روز خودش `bufferSize` را صفر می‌کند؛ اگر کانفیگ را دستی ساخته‌اید این را از قلم نیندازید، وگرنه تونل **بی‌صدا** هیچ داده‌ای رد نمی‌کند. |
| **می‌خواهید مطمئن شوید مشکل از برنامه نیست** | `npm run probe:wg` همین سناریو را با یک peer واقعی سرتاسری اجرا می‌کند و روی هر دو هسته جواب می‌دهد → [آمار و تست](#-آمار-و-تست-stats--testing). |

### گواهی و TLS

| مشکل | راه‌حل |
|---|---|
| **سروری که تا دیروز کار می‌کرد، وصل نمی‌شود** | ممکن است گواهی‌اش عوض شده باشد. برنامه پیش از هر اتصال پین‌ها را می‌سنجد و پینِ کهنه را پاک می‌کند (`Certificate changed for …` در لاگ) و در همان اتصال گواهی تازه را پین می‌کند. اگر می‌خواهید دستی انجامش دهید: فرم ویرایش سرور → **«پاک کردن پین»**. |
| **«این سرور پشت پروکسی است و پینش نمی‌شود»** | یک‌بار **مستقیم** به همان سرور وصل شوید تا گواهی‌اش پین شود؛ بعد در زنجیره هم کار می‌کند. |
| **پین خوانده نشد و در لاگ نوشت** | سرورهایی که فقط uTLS را می‌پذیرند به handshake ساده جواب نمی‌دهند. اشکالی ندارد: اعتبارسنجی گواهی می‌افتد گردنِ خودِ هسته و خطای خودش گویاست. |

---

<div align="center">

ساخته‌ی گروه **IRNetFree** — با ❤️ برای اینترنت آزاد
Built with **Electron** + **xray‑core** · License: MIT

</div>
